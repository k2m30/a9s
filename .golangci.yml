linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused

linters-settings:
  govet:
    enable-all: true
    disable:
      - fieldalignment

issues:
  exclude-dirs:
    - tests/testdata
