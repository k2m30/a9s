# Audit — 2026-04-19: attention-signals + architecture-intended vs. impl

Review of current code against `docs/attention-signals.md` and `docs/architecture-intended.md`.
Output is a triage list — each finding cites the spec clause it violates, a file/line, and a fix sketch.

## Toolchain state (baseline)

| Check | Result |
|---|---|
| `make lint` | PASS (0 issues) |
| `make test` | PASS (unit suite, 7.1s) |
| `make security` | PASS (govulncheck clean) |
| `make gofix` | PASS |
| `.claude/scripts/arch-review.sh` | FAIL — 22 source files >500 lines |

Note: the arch-review script produces no stdout when invoked through the Bash tool in this
session (output drops silently; `bash -x` confirms the script runs but the final `echo "$REPORT"`
reaches nothing in the child stdout). The script's structural checks were re-derived manually
against the codebase; findings appear under "Architecture" below.

---

## A. Wave 1 / Wave 2 signal gaps (attention-signals.md)

These are cases where `attention-signals.md` specifies a signal but the `Color` function in
`internal/resource/types_*.go` or the registered enricher does not implement it. Each row is
a missed row-coloring or missed Wave 2 finding.

| # | shortName | Signal (spec) | Status | Site | Fix |
|---|---|---|---|---|---|
| A1 | `subnet` | `AvailableIpAddressCount / CIDR size < 0.1 → Warning`; `< 0.02 → Broken` | missing | `internal/resource/types_networking.go:122` | fetcher must populate `cidr_total_ips` (from `CidrBlock`) alongside `available_ips`; Color compares ratio |
| A2 | `subnet` | `MapPublicIpOnLaunch=true` without `0.0.0.0/0 → IGW` in associated RTB → Warning | missing | `internal/resource/types_networking.go:122` | Wave 2 cross-ref (needs rtb list); fetcher sets `rtb_has_igw_default` bool on subnet |
| A3 | `vpc` | No subnets → Warning (empty VPC) | missing | `internal/resource/types_networking.go:97` | cross-ref `subnet` list — either precompute in fetcher or add a Wave 2 enricher |
| A4 | `igw` | IGW attached to VPC with no `0.0.0.0/0 → igw` route → Warning (unused) | missing | `internal/resource/types_networking.go:199` | cross-ref `rtb` — fetcher pre-computes `has_default_route` bool from sibling rtb list |
| A5 | `igw` | `Attachments[].State==detached` or `len(Attachments)==0` → Warning | partial | `internal/resource/types_networking.go:199` | Color checks `attachments_count==0`, misses `detached`, `attaching`, `detaching` state surfacing into `state` field |
| A6 | `eip` | Color must also check `NetworkInterfaceId` absent; cross-ref `ec2` stopped instance → Warning | partial | `internal/resource/types_networking.go:227` | add `network_interface_id` to unattached check; cross-ref `ec2` for stopped-instance zombie |
| A7 | `eni` | Requester-managed with `Description` referencing deleted service → Warning (zombie) | missing | `internal/resource/types_networking.go:302` | currently short-circuits requester-managed → Healthy; add description-regex vs. known patterns |
| A8 | `vpce` | `LastError` non-empty → Broken detail; interface endpoint `NetworkInterfaceIds==[]` → Broken; gateway endpoint `RouteTableIds==[]` → Warning | missing | `internal/resource/types_networking.go:247` | fetcher populates `last_error`, `nic_count`, `rtb_count`; Color adds the three rules |
| A9 | `nat` | `FailureCode` non-empty → Broken detail (+ `FailureMessage` surfaced) | missing | `internal/resource/types_networking.go:173` | fetcher writes `failure_code`, `failure_message`; Color promotes to Broken even outside `failed` state |
| A10 | `elb` | State.Reason surfaced as Broken detail | missing | `internal/resource/types_networking.go:21` | fetcher writes `state_reason`; detail-view exposes; Color unchanged |
| A11 | `sg` | Cross-ref `eni` — SG not referenced by any ENI → Warning (orphan) | missing | `internal/resource/types_networking.go:73` | Wave 2 enricher `SGOrphan` — derive from `eni` list; registry currently has no SG enricher |
| A12 | `ami` | Cross-ref `ebs-snap` — backing snapshot missing → Warning (owner-scoped) | missing | `internal/resource/types_compute.go:501` | fetcher populates `backing_snapshot_ids`; Wave 2 cross-ref enricher |
| A13 | `logs` | `retentionInDays` nil → Warning (Never Expire) | missing | `internal/resource/types_monitoring.go:48` | fetcher field `retention_in_days` (sentinel empty vs. "0"); Color adds case |
| A14 | `trail` | `LogFileValidationEnabled==false` → Warning (field is on list response) | missing | enricher registry entry `trail=NoOpEnricher` at `enrichment.go:240` | Wave 1 flag already present on `DescribeTrails` — move to Color, not enricher |
| A15 | `alarm` | `Dimensions` reference a resource ID absent from already-loaded sibling list → Warning (zombie alarm) | missing | `internal/resource/types_monitoring.go:12` | Wave 2 enricher — must honor "skip when sibling list wasn't loaded" clause of the spec |
| A16 | `ct-events` | `Event.ReadOnly=="false"` + `errorCode` (from `Event.CloudTrailEvent` JSON) → Warning | missing | `internal/resource/types_monitoring.go` (no Color for ct-events) | fetcher parses `CloudTrailEvent` JSON on ingest; exposes `error_code`, `read_only`; add Color |
| A17 | `role` | `AssumeRolePolicyDocument` contains `{"AWS":"*"}` without external-id condition → Broken | partial | `internal/resource/types_security.go:23` | current check matches only `"Principal":"*"`; also needs `{"AWS":"*"}` form with condition inspection |
| A18 | `tgw` | Attachment `State in failed/failing/rejected/rejecting` → Broken (Wave 2 via `DescribeTransitGatewayAttachments`) | missing | `internal/resource/types_networking.go:274` | no TGW enricher; add one, or promote attachment-state via prefetch |
| A19 | `cfn` | `IN_PROGRESS > 1h` → Broken (stuck) | missing | enricher at `enrichment.go:184` | existing `EnrichCFNCombined` handles drift + event scan; extend to detect `*_IN_PROGRESS` with `CreationTime`/`LastUpdatedTime` older than 1h |
| A20 | `rds-snap` | Age > parent `BackupRetentionPeriod` AND `SnapshotType==automated` → Warning | missing | fetcher `rds_snapshots.go` | when parent DB is in loaded list, cross-ref; needs Wave 2 enricher or post-merge step |
| A21 | `docdb-snap` | Age > parent cluster `BackupRetentionPeriod` × 1.5 AND automated → Warning | missing | fetcher `docdb_snapshots.go` | same shape as A20 |
| A22 | `ecr` | `imageScanningConfiguration.scanOnPush==false` → Warning | missing | `internal/resource/types_containers.go`/ecr type def | single-field check; fetcher must surface scan_on_push |
| A23 | `ses` | Wave 2 account-wide: `EnforcementStatus` in PROBATION/SHUTDOWN → Broken; `SendQuota.SentLast24Hours > 0.8 × Max24HourSend` → Warning | unverified | `internal/aws/ses.go` | check whether account-wide `GetAccount` enricher is registered; if absent, add |

**Verification notes for A1–A23:** the Wave-1/Wave-2 audit agent flagged several signals as "missing"
that were actually Wave 2 (e.g. SQS queue depth, SNS orphan topic) — those are correctly implemented as
enrichers (`enrichment.go:178`–`179`). The table above is filtered to real gaps.

---

## B. Architectural divergence (architecture-intended.md)

| # | Spec clause | Violation | Severity | Site | Fix |
|---|---|---|---|---|---|
| B1 | §Principle 6 "Background systems must be registry-complete. Hand-maintained allowlists for supported types are architectural debt." | `injectEnrichmentSection` dispatches per-type with a `switch m.resourceType {…}` and 7 hand-maintained cases | **high** | `internal/tui/views/detail_fields.go:551` | promote section label + path key into `resource.EnricherDef` (or sibling registry) so the detail view reads label from registry; delete the switch |
| B2 | §Rule 5 "Every async result must carry enough identity to reject stale, cross-resource, cross-profile, or cross-region updates." | `ResourcesLoadedMsg` has no `Gen` / profile / region — fetches dispatched under profile A and completed after switch to profile B get merged into B's cache via `handleResourcesLoaded` | **high** | `internal/tui/messages/messages.go:35` + `internal/tui/app.go:458` | add `Gen int` (stamped with `m.connectGen` at dispatch in `app_fetchers.go`); guard in `case messages.ResourcesLoadedMsg` parallel to `ClientsReadyMsg`/`EnrichmentCheckedMsg` |
| B3 | §Resource Model + §Related Panel: `RelatedDef.ResourceIDs` MUST be IDs valid for declared `TargetType` | no automated test validates this; regressions can only be caught in integration | medium | `tests/unit/*related*` | add a reflection-based unit test: for each `RelatedDef`, call checker against a fixture resource and assert returned IDs are parseable as the target type's canonical ID (or at least not a value that could not be present in the target list) |
| B4 | §Layer Boundaries `internal/aws` owns AWS adapter logic | `internal/aws/enrichment.go` is 4,197 lines containing ~40 enrichers + `Enricher`, `NoOpEnricher`, `EnricherRegistry` all in one file — god-file | medium | `internal/aws/enrichment.go` | split per-service: `enrichment_rds.go`, `enrichment_ebs.go`, `enrichment_s3.go`, …; move `Enricher` / `EnricherRegistry` / priorities into a small `enricher_registry.go` |
| B5 | §Layer Boundaries | `internal/aws/interfaces.go` is 1,542 lines with all AWS service-API interfaces in one file | low | `internal/aws/interfaces.go` | split per-service into `interfaces_rds.go`, `interfaces_ec2.go`, …; `interfaces_networking.go` already exists, extend the pattern |
| B6 | §Root Runtime Model + Practical Checklist "every type derives behavior from registries" | no lint / compile-time check that a new `ResourceTypeDef` has a corresponding enricher entry (real or `NoOpEnricher`) | low | registry package | add a test that asserts `EnricherRegistry` has an entry (possibly `NoOpEnricher`) for every `ResourceTypeDef.ShortName` |
| B7 | §Practical Checklist "Do refresh/profile/region transitions invalidate the correct state?" | verified: `handleProfileSelected`/`handleRegionSelected` clear `relatedCache`, bump 5 generation counters, drop `resourceCache`, `enrichmentFindings`, `enrichmentRan`, `enrichmentTypeGen`, `enrichmentTruncatedIDs`, `enrichQueue`, `probeResources` — **conforms** | — | `internal/tui/app_handlers.go:319` | no action |
| B8 | §Wave 2 "the background enrichment queue MUST be built from the full registry" | verified: `buildEnrichQueue` iterates `awsclient.EnricherRegistry` directly, sorts by priority — **conforms** | — | `internal/tui/app_fetchers.go:546` | no action |
| B9 | §Demo Mode "Demo mode is an injected runtime mode, not a parallel architecture" | verified: `m.isDemo` is a flag, suppresses only the Wave 2 enricher dispatch — **conforms** | — | `cmd/a9s/main.go:173` / `app_handlers_navigate.go:395` | no action |
| B10 | §Layer Boundaries `views` MUST NOT embed AWS service-call logic | verified: no `aws/` imports from `internal/tui/views/*.go` — **conforms** | — | — | no action |

---

## C. File-size / god-object findings

From `arch-review.sh` (reconstructed). Threshold: 500 lines for non-test, non-demo-fixture code.

| File | Lines | Note |
|---|---|---|
| `internal/aws/enrichment.go` | 4,197 | B4 — split per service |
| `internal/aws/interfaces.go` | 1,542 | B5 — split per service |
| `internal/tui/app.go` | 889 | root Model — monitor |
| `internal/aws/ct_events_related.go` | 869 | CloudTrail pivot file — consider splitting per resource pivot |
| `internal/aws/ct_events.go` | 868 | CloudTrail fetcher — consider splitting field-extraction into helpers |
| `internal/tui/app_handlers_navigate.go` | 864 | all navigation handlers — split by target kind (detail vs list vs child) |
| `internal/tui/views/detail_fields.go` | 779 | will shrink when B1 is fixed |
| `internal/tui/app_fetchers.go` | 619 | 3 fetch modes + wave-2 probe dispatch — consider split |
| `internal/tui/views/resourcelist.go` | 609 | large view, but single responsibility |
| `internal/tui/views/mainmenu.go` | 609 | large view, but single responsibility |
| `internal/aws/lambda_related_extra.go` | 585 | lambda related checkers batch |
| `internal/fieldpath/extract.go` | 564 | reflection-based field extraction |
| `internal/tui/views/resourcelist_helpers.go` | 555 | helpers — consider consolidating into resourcelist.go or splitting by concern |
| `internal/aws/s3_related.go` | 555 | s3 related checkers |
| `internal/resource/types_compute.go` | 548 | all compute type defs — acceptable, single table |
| `internal/aws/ec2_related.go` | 546 | ec2 related checkers |
| `internal/tui/views/help.go` | 516 | help text — acceptable |
| `internal/tui/app_handlers.go` | 505 | root update dispatch — likely acceptable |

Immediate action: **B4 (enrichment.go split) + B1 (remove detail-field switch)** — together these
will drop 4 files from the >500-line list.

---

## D. AI-slop / code-smell

Rejected bug-hunter claims that did not hold up under spot-check (documented so we don't waste
time re-investigating):

- `acm_related.go:126-127` — *claimed* unguarded index. Actually guarded by
  `idx >= 0 && idx < len(arn)-1`. Not a bug.
- `identity.go:87` — *claimed* panic on empty `segments`. Actually gated by
  `strings.HasPrefix(resourcePart, "user/")`, so `Split` yields ≥2 elements. Not a bug.
- `ApproximateZero` usage — *claimed* missing truncation validation. Helper is used
  correctly by conventions in `*_related.go` (`truncated && len(ids)==0`). Not a bug.

Real smells worth addressing:

| # | Issue | Severity | Site | Status |
|---|---|---|---|---|
| D1 | `errNoR53Client` sentinel reused by ACM / APIGW checkers as a generic "client not initialized" marker | low | `internal/aws/{r53,acm,apigw}_related.go` | **DONE** (renamed to `errClientMissing`, 17 sites, 2026-04-19) |
| D2 | `acmCertInUseBy` uses the `(nil, nil)` return pattern for two distinct cases | low | `internal/aws/acm_related.go:61,74` | **DONE** (returns `([]string{}, nil)` for empty input, 2026-04-19) |
| D3 | `ct_events.go` / `ct_events_related.go` both ~870 lines and largely parallel — CloudTrail event parsing and pivoting share logic | medium | `internal/aws/ct_events*.go` | open — lift JSON field extraction into a helper package (e.g. `ct_events_target.go` already hints at this — expand) |

---

## E. Suggested sequencing

Three batches, ordered by impact-per-effort:

### Batch 1 — correctness (signals)

1. A13 (logs `retentionInDays` nil → Warning) — one-field Color change
2. A14 (trail `LogFileValidationEnabled` → Warning) — move from NoOp enricher to Color
3. A17 (role Principal wildcard-with-AWS-map check) — regex upgrade
4. A22 (ecr `scanOnPush==false`) — one-field Color change
5. A6 (eip add `network_interface_id` + stopped-EC2 cross-ref)
6. A9 (nat `FailureCode` → Broken)
7. A10 (elb state_reason surfaced)
8. A1 (subnet IP exhaustion ratio) — needs fetcher field addition
9. A8 (vpce LastError / NetworkInterfaceIds / RouteTableIds)

### Batch 2 — architecture

1. B2 (ResourcesLoadedMsg Gen guard) — load-bearing correctness fix
2. B1 (detail_fields.go switch → registry) — removes 60 lines of AI-slop, prevents drift on new enrichers
3. B4 (split enrichment.go by service) — mechanical refactor, unblocks reviewability

### Batch 3 — cross-ref signals (coordinated)

1. A3 / A4 (vpc empty / igw unused cross-ref) — coordinated via sibling-cache pattern
2. A11 (sg orphan via eni) — new Wave 2 enricher
3. A15 (alarm zombie dimension) — new Wave 2 enricher with "skip when sibling list wasn't loaded" clause from spec
4. A12 (ami backing snapshot) — requires owner-scope filter per spec
5. A20 / A21 (rds-snap / docdb-snap orphan by parent retention) — paired

### Batch 4 — low-priority hygiene

1. B3 (related-def ID-type reflection test)
2. B5 (split interfaces.go)
3. B6 (lint for `ResourceTypeDef` without enricher entry)
4. D1 (rename `errNoR53Client`) — DONE 2026-04-19
5. D2 (`acmCertInUseBy` nil-return cleanup) — DONE 2026-04-19
6. A2 / A18 / A19 (remaining Wave 2 gaps)

---

## F. Out of scope for this pass

- `docs/related-resources.md` contract compliance — not reviewed here; that doc is governed by
  its own single-source-of-truth rule and deserves a separate audit.
- Integration-test coverage of Wave 2 enrichers against real AWS — baseline `make test` passes;
  the pre-push full-validation integration test (`A9S_CT_PROFILE`) was not run.
- CHANGELOG / release-notes alignment — this review is not release-gated.
