---
shortName: s3
name: S3 Buckets
awsApiRef: https://docs.aws.amazon.com/AmazonS3/latest/API/API_ListBuckets.html
generatedFrom:
  - docs/architecture.md
  - docs/related-resources.md
  - docs/attention-signals.md
  - docs/historical/analysis/enrichment-visibility.md
---

# s3 — Resource Spec

Golden UX/UI doc for this resource, written from the operator's perspective. Describes what the list row, Status column, glyphs, and detail view should look like — the should-be, not the is. Implementation conforms to this doc; tests assert against it. When code and this doc disagree, the code is wrong.

## 1. Identity

- **shortName**: `s3`
- **Display name**: S3 Buckets
- **AWS API reference**: <https://docs.aws.amazon.com/AmazonS3/latest/API/API_ListBuckets.html>
- **List API**: `ListBuckets` (returns per-bucket `Name`, `CreationDate`, `BucketRegion`, `BucketArn` only — no state field).
- **Describe API (if any)**: `GetPublicAccessBlock` (per bucket; used in Wave 2).

## 2. Related Resources Panel (detail view, right column)

Expected targets from `docs/related-resources.md` Per-type contract (as amended 2026-04-22): `athena`, `backup`, `cf`, `cfn`, `eb-rule`, `glue`, `kms`, `lambda`, `s3` (access-log destination), `r53`, `role`, `sns`, `sqs`, `trail`, `ct-events`. The contract's original draft also listed `iam-user`, `logs`, and `waf`: `iam-user` and `waf` were removed from the contract on 2026-04-22 (struck-through entries there carry the rationale; see also §5), and `logs` was renamed to the `s3` access-log entry.

**Interpretation note for the access-log pivot**: for S3, "logs" never meant CloudWatch Log Groups — S3 server-access logs are delivered to another S3 bucket. The pivot is registered as `s3` (DisplayName "Access Log Bucket") so navigation lands on the destination bucket's S3 row. See the `logs` subsection below for full reasoning and the a9s-devops citation.

### `athena`

- **Why related**: Athena queries read/write against S3 — this bucket may be the data source for scans or the query-result output location.
- **How discovered**: Reverse-scan the already-loaded `athena` list — inspect each workgroup's `ResultConfiguration.OutputLocation` (an `s3://bucket/prefix` URI) and match the bucket segment against this bucket's `Name`. Data-source buckets referenced by user queries are not on the workgroup object and are out of scope. — a9s-devops: workgroup output-location is the only AWS-surface link; table-level data-source mapping requires Glue Data Catalog walking which is Wave 3.
- **Count shown**: yes.

### `backup`

- **Why related**: This bucket may be protected by an AWS Backup plan — operator wants to confirm the bucket is covered before declaring a recovery posture.
- **How discovered**: Reverse-scan the already-loaded `backup` list. For each plan, check whether the bucket's ARN (`arn:aws:s3:::<bucket-name>`) is covered by `BackupSelection.Resources` (with AWS wildcard semantics — e.g. `arn:aws:s3:::*`) and not excluded by `BackupSelection.NotResources`. A plan covers this bucket iff any Resources entry matches AND no NotResources entry matches. Recovery-point-shaped cache entries are matched via a direct `resource_arn` field equality check (separate legacy path). — a9s-devops: Backup coverage lives on the plan's selection, not on the bucket. Reverse-scan against the already-loaded `backup` list is the cheapest approach for a9s.
- **Count shown**: yes.

### `cf`

- **Why related**: CloudFront distributions may serve this bucket as an origin — investigating viewer-facing latency or 403s starts from the bucket and jumps to the distribution.
- **How discovered**: Reverse-scan the already-loaded `cf` list — for each distribution, walk `DistributionConfig.Origins.Items[].DomainName` and match `<bucket>.s3.<region>.amazonaws.com` or `<bucket>.s3-website-<region>.amazonaws.com` against this bucket's `Name`. — a9s-devops: origin list is the definitive field; cite AWS API Reference API_Origin.DomainName.
- **Count shown**: yes.

### `cfn`

- **Why related**: The CloudFormation stack that provisioned this bucket is the fastest route to the template, parameters, and stack events — critical for drift and change history.
- **How discovered**: Read the bucket's `aws:cloudformation:stack-name` tag (set automatically on CFN-managed resources) via `s3:GetBucketTagging` and look up the stack in the already-loaded `cfn` list. — a9s-devops: the tag is the canonical CFN-managed marker; no bucket tag → not CFN-managed (skip).
- **Count shown**: yes.

### `eb-rule`

- **Why related**: EventBridge rules may fire on S3 object events (created/deleted) — this bucket may be the source triggering downstream automation.
- **How discovered**: Reverse-scan the already-loaded `eb-rule` list — rules with `EventPattern.source=["aws.s3"]` AND `EventPattern.detail.bucket.name` matching this bucket. — a9s-devops: event-pattern field is standard for S3-originated EventBridge rules; cite AWS Events User Guide §S3 event notifications.
- **Count shown**: yes.

### `glue`

- **Why related**: Glue crawlers populate the Data Catalog from S3 — operator debugging a stale table wants to hop to the crawler.
- **How discovered**: Call `glue:GetCrawlers` and inspect each crawler's `Targets.S3Targets[].Path` for a prefix matching `s3://<bucket>`. This is a separate API (not on the bucket). — a9s-devops: S3 targets live on the crawler, not on the bucket; `GetCrawlers` is account-wide and cheap.
- **Count shown**: yes.

### `iam-user` (removed from contract 2026-04-22)

- **Why related**: Audit pivot — when triaging a bucket-access incident operator wants to see the IAM users that appear on principal-level access logs or ownership (e.g. `Owner.DisplayName`). — a9s-devops: not worth it, see §5 Out of Scope.
- **How discovered**: `TBD — a9s-devops: not available in AWS surface for an operator-grade pivot; bucket-level principal attribution requires CloudTrail data-plane parsing, which is Wave 3. Owner is an account ID, not a user.`
- **Count shown**: unknown.

### `kms`

- **Why related**: Bucket is encrypted with a customer-managed KMS key via SSE-KMS — operator wants to confirm the key is enabled and not pending deletion before declaring the bucket usable.
- **How discovered**: Call `s3:GetBucketEncryption` and read `ServerSideEncryptionConfiguration.Rules[].ApplyServerSideEncryptionByDefault.KMSMasterKeyID`; look the ARN up in the already-loaded `kms` list. — a9s-devops: `GetBucketEncryption` is the canonical source; AWS-managed `aws/s3` keys map to a standard alias — treat them as "managed default" in the panel.
- **Count shown**: yes.

### `lambda`

- **Why related**: Lambda functions may be subscribed to S3 object events on this bucket — operator chasing an event-driven failure starts here.
- **How discovered**: Call `s3:GetBucketNotificationConfiguration` and read `LambdaFunctionConfigurations[].LambdaFunctionArn`; look the ARN up in the already-loaded `lambda` list. — a9s-devops: notification configuration is the definitive bucket-side source; reverse-scanning lambda event-source-mappings will miss S3-as-source because ESMs are for Kinesis/SQS/DynamoDB.
- **Count shown**: yes.

### `logs` (implemented as `s3` — "Access Log Bucket")

- **Why related**: S3 server-access logs are delivered to another S3 bucket, not CloudWatch Logs. The contract entry was originally drafted as `logs` (misleading shorthand for "log-destination bucket") and renamed to `s3` on 2026-04-22 — the pivot is registered with `TargetType="s3"` and `DisplayName="Access Log Bucket"` so navigation lands on the destination bucket's S3 row, not on an empty CloudWatch Log Groups list.
- **How discovered**: Call `s3:GetBucketLogging` → `LoggingEnabled.TargetBucket`; the target IS another S3 bucket. Emitted by `checkS3Logs` as `TargetType="s3"` with the destination bucket name as the navigation ID. Reverse direction (buckets shipping logs HERE) is not currently implemented. — a9s-devops: S3 access logs never go to CloudWatch Logs. Worth surfacing — operators chasing retention/cost/forensics hit this regularly.
- **Count shown**: yes.

### `r53`

- **Why related**: A Route 53 alias record may point to this bucket's website endpoint — chasing a `NoSuchBucket` or 404 for a custom domain lands here.
- **How discovered**: Reverse-scan the already-loaded `r53` list — for each hosted zone, list `ResourceRecordSets` where `AliasTarget.DNSName` matches `s3-website-<region>.amazonaws.com.` AND the record name matches this bucket's `Name` (website-endpoint convention requires bucket name == FQDN). — a9s-devops: alias to S3 requires bucket-name==FQDN; that's the join key. Cite AWS Route 53 Developer Guide §Routing-traffic-to-website.
- **Count shown**: yes.

### `role`

- **Why related**: Audit pivot — when the bucket policy references an `AWS` principal that is an IAM role ARN, operator wants to hop to that role. — a9s-devops: possible but low value for a daily-driver — bucket policies are typically maintained in IaC and the role link is better discovered from the role side.
- **How discovered**: Call `s3:GetBucketPolicy`, parse the JSON policy document for `Statement[].Principal.AWS` entries matching IAM role ARNs, look each up in the already-loaded `role` list. — a9s-devops: possible via bucket-policy parsing but fragile (wildcard principals, service principals, cross-account IDs) — worth keeping as an advanced pivot, not a first-order one.
- **Count shown**: yes.

### `sns`

- **Why related**: Bucket event notifications may publish to an SNS topic — operator chasing a fan-out notification path starts here.
- **How discovered**: Call `s3:GetBucketNotificationConfiguration` and read `TopicConfigurations[].TopicArn`; look the ARN up in the already-loaded `sns` list. — a9s-devops: same single API as Lambda notifications; one call covers Lambda + SNS + SQS destinations.
- **Count shown**: yes.

### `sqs`

- **Why related**: Bucket event notifications may enqueue to an SQS queue — operator chasing a stuck consumer or backlog starts here.
- **How discovered**: Call `s3:GetBucketNotificationConfiguration` and read `QueueConfigurations[].QueueArn`; look the ARN up in the already-loaded `sqs` list. — a9s-devops: same single-call source as SNS/Lambda notifications.
- **Count shown**: yes.

### `trail`

- **Why related**: This bucket may be a destination for one or more CloudTrail trails — operator investigating a trail-delivery failure jumps to the trails targeting this bucket.
- **How discovered**: Reverse-scan the already-loaded `trail` list — match `Trail.S3BucketName == this.Name`. — a9s-devops: `S3BucketName` is the canonical field on `DescribeTrails`; reverse-scan is one pass over the trail list with zero extra API calls.
- **Count shown**: yes.

### `waf` (removed from contract 2026-04-22)

- **Why related**: Audit pivot — a WAF web ACL does not directly attach to an S3 bucket (ACLs attach to CloudFront, ALB, API Gateway, AppSync, Cognito). The link is indirect, via a CloudFront distribution fronting this bucket. — a9s-devops: not worth it as a first-order S3 pivot; better reached through `cf`. See §5 Out of Scope.
- **How discovered**: `TBD — a9s-devops: not available in AWS surface as a direct S3→WAF edge; the only path is S3→CloudFront→WAF which is already reachable via the`cf`panel entry.`
- **Count shown**: unknown.

### `ct-events`

- **Why related**: Universal pivot — applies to every registered type; see `related-resources.md` §Policy.
- **How discovered**: CloudTrail `LookupEvents` with `LookupAttribute=ResourceName` = bucket name (and/or `ResourceARN` = `arn:aws:s3:::<bucket>`).
- **Count shown**: yes.

## 3. Attention / Issues Algorithm

**Source API**: [GetPublicAccessBlock](https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetPublicAccessBlock.html)

Transcribed from `docs/attention-signals.md § Signals § DATABASES & STORAGE` row `s3`.

### 3.1 Wave 1 — zero extra API calls

No health signal on the list response — `ListBuckets` returns `Name`, `CreationDate`, `BucketRegion`, `BucketArn` only, and there is no lifecycle / state / health field to classify from.

### 3.2 Wave 2 — bounded extra API calls

- **Signal**: `GetPublicAccessBlock` per bucket returns `NoSuchPublicAccessBlockConfiguration` error OR any of `BlockPublicAcls` / `IgnorePublicAcls` / `BlockPublicPolicy` / `RestrictPublicBuckets` is `false`.
  - **State bucket**: Healthy (the row stays green — S3 has no Wave 1 color signal; the finding is a background-check annotation, not a primary row state).
  - **API call**: `GetPublicAccessBlock` — one per bucket.
  - **Cost shape**: per-resource.

### 3.3 Wave 3 — OUT OF SCOPE

- OUT OF SCOPE: `GetBucketPolicyStatus`
- OUT OF SCOPE: `GetBucketEncryption`
- OUT OF SCOPE: `GetBucketVersioning`
- OUT OF SCOPE: `GetBucketLogging`
- OUT OF SCOPE: `GetBucketLifecycleConfiguration` (each is per-bucket)

## 4. Issue Visualization

Every signal from §3.1 and §3.2 must land on one or more of these five existing surfaces. No other UI is allowed.

| # | Surface | Mechanism |
|---|---|---|
| S1 | Menu `issues:N` count + list frame title `!N` suffix | Aggregated count of `!`-severity findings. `~` findings do not bump. The list frame title appends a space-separated `!N` after the count parentheses when the current list has N > 0 issues (`s3(50+) !5`, `ec2(17) !1`), or `!N+` when N is a truncated lower bound; N uses the same aggregation as the menu badge; the generated note under this table says which waves feed it for this type. No suffix when N = 0, and omitted in attention-only mode (`ctrl+z`) — the filtered count already is the issue count, so `name(5 of 50+) [!]` stays as-is. |
| S2 | Row color (list view) | Row colored by state bucket — Healthy=green, Warning=yellow, Broken=red, Dim=gray. Yellow/red/dim are themselves the attention signal. |
| S3 | `!` / `~` glyph before the name | Annotates a Healthy (green) row with "no immediate action, but worth knowing" — e.g. maintenance scheduled, certificate expiring soon. `!` = important background concern, `~` = informational. **Never appears on yellow/red/dim rows.** |
| S4 | Status / description column text | Short human-readable cause (e.g. `public access block missing`). **Healthy rows render blank** — no `OK` / `available` / `ACTIVE` / `running`. Empty means "nothing to see." |
| S5 | Detail view enrichment line | Short operator-readable sentence rendered inline in the detail view. No ceremonial header. |

<!-- BEGIN GENERATED: badge -->
Badge aggregation for `s3`: Wave 1 issue-colored rows plus Wave 2 `!`-severity findings — this type registers a Wave 2 enricher.
<!-- END GENERATED: badge -->

Wave → surface mapping applied here:

- S3 has no Wave 1 signal, so every non-findings row stays green with a blank Status — silence is the UX.
- The Wave 2 PAB finding is a Broken-style background concern on a Healthy row → `!` glyph, S1 + S3 + S4 + S5.

One row per signal from §3:

| Signal (short) | Wave | State bucket | Severity | Surfaces reached | List text (S4) |
|---|---|---|---|---|---|
| `GetPublicAccessBlock` missing or any PAB flag false | 2 | Healthy | `!` | S1, S3, S4, S5 | `public access block incomplete` |
| `GetBucketPolicyStatus.IsPublic` | 2 | Broken | `!` | S1, S2, S4, S5 | `publicly accessible` |
| Versioning not `Enabled` | 2 | Warning | `~` | S2, S4, S5 | `versioning off` |
| Versioning on, MFA delete not `Enabled` | 2 | Warning | `~` | S2, S4, S5 | `MFA delete off` |
| `GetBucketLogging` reports no `LoggingEnabled` | 2 | Warning | `~` | S2, S4, S5 | `access logging off` |
| Zero enabled lifecycle rules | 2 | Warning | `~` | S2, S4, S5 | `no lifecycle rules` |
| Object lock not `Enabled` | 2 | Warning | `~` | S2, S4, S5 | `object lock off` |

## 4.1 UX review

At 3am, glancing at the list, can the operator tell what's wrong with a problem row without opening detail? Yes for this resource — any bucket flagged with `!` carries `public access block incomplete` in the Status column, which names the concern plainly. The detail line adds the single nuance an operator needs (account-level PAB may override) without forcing a navigation. All problem rows are self-explanatory in the list — operator can triage without opening detail.

## 4.2 On-Demand Detail Enrichment

Opening the detail, YAML, or JSON view triggers up to three extra read-only calls whose results are attached to the resource's raw structure for the stacked views. List views are never affected.

- AWS APIs: `GetBucketPolicy`, `GetBucketCors`, `GetBucketLifecycleConfiguration`
- Payload: the bucket policy document (structured YAML), CORS rules, and lifecycle rules; a bucket without one of these configurations simply omits that block. The policy document also renders as the last detail-view field; CORS and lifecycle live on the YAML/JSON views
- Cache: none — cheap calls, and bucket configuration can change mid-session
- Distinct from §5: those exclusions reject Wave-3 *signals* computed from these APIs; this section only displays the documents on demand, no signal is derived
- Failure: a flash message; the view still renders the un-enriched resource

## 5. Out of Scope

- `GetBucketEncryption` as a *signal* — the detail view surfaces the encryption configuration, but no finding is derived from it.
- `iam-user` related-panel entry — discovery mechanism not available in AWS surface for an operator-grade pivot (principal attribution requires CloudTrail data-plane parsing, which is Wave 3). a9s-devops: not worth it.
- `waf` related-panel entry as a first-order S3 pivot — WAF does not attach to S3 directly; the indirect path goes through CloudFront and is already covered by the `cf` panel entry. a9s-devops: not worth it.
- Any UI element not listed in §4 — e.g. new columns, new icons, new views, new key bindings.
- Any write operation. a9s is read-only by design (`architecture.md` §"What is a9s?").

## 6. Citations

- a9s golden doc — Per-type related contract for `s3` — `docs/related-resources.md` § `Per-type contract` and § `### s3`.
- a9s golden doc — the `s3` signals — `docs/attention-signals.md § Signals § DATABASES & STORAGE` row `s3`; the deferred `GetBucketEncryption` read — `docs/attention-signals.md § Not yet implemented`.
- a9s golden doc — `!` severity, red row marker, `public access block incomplete` summary for S3 Wave 2 — `docs/historical/analysis/enrichment-visibility.md` § `Tier Classification` (S3 / `GetPublicAccessBlock` / `!` / `"public access block incomplete"`).
- a9s golden doc — `ct-events` universal pivot — `docs/related-resources.md` § `Policy` point 4.
- a9s golden doc — read-only invariant — `docs/architecture.md` § `What is a9s?`.
- AWS API Reference — `ListBuckets` returns only `Name`, `CreationDate`, `BucketRegion`, `BucketArn` per bucket — `AWS API Reference: ListBuckets § Bucket` (<https://docs.aws.amazon.com/AmazonS3/latest/API/API_ListBuckets.html>).
- AWS API Reference — `PublicAccessBlockConfiguration` fields (`BlockPublicAcls`, `IgnorePublicAcls`, `BlockPublicPolicy`, `RestrictPublicBuckets`) — `AWS API Reference: GetPublicAccessBlock § PublicAccessBlockConfiguration` (<https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetPublicAccessBlock.html>).
- a9s-devops consultation — `athena` discovery via workgroup `ResultConfiguration.OutputLocation` — `a9s-devops (2026-04-20): possible=yes, worth=yes. Workgroup output-location is the only AWS-surface link; data-source table mapping is Wave 3.`
- a9s-devops consultation — `backup` discovery via selection scan with wildcard + NotResources matching — `a9s-devops (2026-04-20): possible=yes, worth=yes. Coverage lives on the plan's selection; plans may use wildcard ARNs (e.g. arn:aws:s3:::*) and NotResources exclusions; reverse-scan against the already-loaded backup list is cheapest.`
- a9s-devops consultation — `cf` discovery via `DistributionConfig.Origins.Items[].DomainName` — `a9s-devops (2026-04-20): possible=yes, worth=yes. Origin list is the definitive field on the distribution.`
- a9s-devops consultation — `cfn` discovery via `aws:cloudformation:stack-name` bucket tag — `a9s-devops (2026-04-20): possible=yes, worth=yes. Tag is the canonical CFN-managed marker; needs s3:GetBucketTagging.`
- a9s-devops consultation — `eb-rule` discovery via rule `EventPattern.source=aws.s3` + `detail.bucket.name` — `a9s-devops (2026-04-20): possible=yes, worth=yes. Event-pattern field is standard for S3-originated EventBridge rules.`
- a9s-devops consultation — `glue` discovery via `GetCrawlers.Targets.S3Targets[].Path` — `a9s-devops (2026-04-20): possible=yes, worth=yes. S3 targets live on the crawler, not the bucket; GetCrawlers is cheap and account-wide.`
- a9s-devops consultation — `iam-user` discovery not feasible — `a9s-devops (2026-04-20): possible=no, worth=no. Principal attribution requires CloudTrail data-plane parsing (Wave 3). Owner is an account ID, not a user.`
- a9s-devops consultation — `kms` discovery via `GetBucketEncryption.ServerSideEncryptionConfiguration.Rules[].ApplyServerSideEncryptionByDefault.KMSMasterKeyID` — `a9s-devops (2026-04-20): possible=yes, worth=yes. Canonical source; AWS-managed aws/s3 alias treated as managed default.`
- a9s-devops consultation — `lambda`, `sns`, `sqs` discovery via `GetBucketNotificationConfiguration` — `a9s-devops (2026-04-20): possible=yes, worth=yes. One API covers all three destination types (LambdaFunctionConfigurations, TopicConfigurations, QueueConfigurations).`
- a9s-devops consultation — `logs` entry clarification (S3→S3 access-log destination, not CloudWatch Logs) — `a9s-devops (2026-04-20): possible=yes, worth=yes. S3 access logs never go to CloudWatch; the entry means S3 access-log-destination bucket, reachable via GetBucketLogging.LoggingEnabled.TargetBucket.`
- a9s-devops consultation — `r53` discovery via `AliasTarget.DNSName` matching `s3-website-<region>.amazonaws.com` with bucket-name==FQDN — `a9s-devops (2026-04-20): possible=yes, worth=yes. Alias to S3 requires bucket-name == FQDN; that's the join key.`
- a9s-devops consultation — `role` discovery via `GetBucketPolicy` principal parsing — `a9s-devops (2026-04-20): possible=yes, worth=partial. Fragile (wildcards, service principals, cross-account IDs); keep as advanced pivot only.`
- a9s-devops consultation — `trail` discovery via reverse-scan for `Trail.S3BucketName == this.Name` — `a9s-devops (2026-04-20): possible=yes, worth=yes. Canonical field on DescribeTrails; zero extra API calls.`
- a9s-devops consultation — `waf` direct S3→WAF edge not available — `a9s-devops (2026-04-20): possible=no, worth=no. WAF attaches to CloudFront / ALB / APIGW / AppSync / Cognito, not S3; the indirect path is S3→cf→waf and is already covered by the cf entry.`

<!-- BEGIN GENERATED: header -->
s3 — DATABASES & STORAGE. Lifecycle key: none (the list API returns no lifecycle field).
<!-- END GENERATED: header -->

<!-- BEGIN GENERATED: findings -->
| Code | Phrase | Severity | Source | Detail |
| --- | --- | --- | --- | --- |
| s3.public-access-block-incomplete | public access block incomplete | warn | wave2 | Bucket-level public access block is missing or partial — account-level PAB may still apply. |
| s3.public | publicly accessible | broken | wave2 | AWS reports this bucket's policy as public, so anyone on the internet can reach its objects. Remove the wildcard-principal statements from the bucket policy, or block them with a public access block. |
| s3.versioning-off | versioning off | warn | wave2 | Overwritten and deleted objects are gone for good — there is no previous version to restore. Enable versioning on the bucket. |
| s3.mfa-delete-off | MFA delete off | warn | wave2 | Versioning is on, but anyone holding the delete permission can still remove versions permanently. Enable MFA delete so destroying a version needs a second factor. |
| s3.access-logging-off | access logging off | warn | wave2 | Nothing records who read or wrote objects here, so an incident leaves no trail to follow. Point server access logging at a log destination bucket. |
| s3.no-lifecycle | no lifecycle rules | warn | wave2 | No lifecycle rule expires or transitions objects, so data and cost accumulate indefinitely. Add a lifecycle rule matching the bucket's retention policy. |
| s3.no-object-lock | object lock off | warn | wave2 | Objects can be overwritten or deleted by anyone with write access — nothing enforces retention. Enable object lock on a new bucket and migrate if the data is compliance-relevant. |
<!-- END GENERATED: findings -->

<!-- BEGIN GENERATED: related -->
| Target Type | Display Name | Truncated? |
| --- | --- | --- |
| trail | CloudTrail Trails | yes |
| cf | CloudFront | yes |
| lambda | Lambda (notifications) | no |
| sns | SNS (notifications) | no |
| sqs | SQS (notifications) | no |
| cfn | CloudFormation | yes |
| kms | KMS Key | yes |
| s3 | Access Log Bucket | yes |
| athena | Athena WorkGroups | yes |
| glue | Glue Jobs | yes |
| backup | Backup | yes |
| eb-rule | EventBridge Rules | yes |
| r53 | Route 53 | yes |
| role | IAM Roles | yes |
| ct-events | CloudTrail Events | no |
<!-- END GENERATED: related -->
