# Test Scoring

Auto-generated by `scripts/aggregate_test_scores.py`. 393 files. Sorted ascending by score (weakest first — review for deletion / lightening).

Tracking issue: [#248 — Strengthen test suite: delete busywork, upgrade lazy stubs, land pending TDD](https://github.com/k2m30/a9s/issues/248)

| File | Score | Reasoning | What's Missing | Action Plan |
|------|-------|-----------|----------------|-------------|
| `tests/unit/helpers_external_test.go` | 15 | Same as above for `unit_test` package — pure helpers. | Infrastructure only. | **keep (infra)** — shared by qa_detail/qa_list_rawstruct/qa_s3_object_detail; do not score. |
| `tests/unit/helpers_test.go` | 15 | Pure test helpers (ANSI strip, lipgloss width); no test functions, just shared utilities. | Not independently scoreable — infrastructure only. | **keep (infra)** — required by internal-package tests. Exclude from scoring in `aggregate_test_scores.py`. |
| `tests/unit/mocks_services_test.go` | 20 | Additional mock AWS service clients — same as above. | Infrastructure only. | **keep (infra)** — mock client definitions reused by fetcher tests. Exclude from scoring. |
| `tests/unit/mocks_test.go` | 20 | Mock AWS client implementations — no test functions, pure test infrastructure. | Infrastructure only. | **keep (infra)** — central mock registry. Exclude from scoring. |
| `tests/unit/aws_athena_related_test.go` | 22 | Both checkers are undeterminable stubs returning Count:0. Tests only verify registration and the stub return value — no real logic to catch bugs in. |  | **verify + shrink or upgrade**: (1) verify athena→s3/kms are actually undeterminable (WorkGroupSummary lacks OutputLocation/KmsKey — likely legit); if GetWorkGroup N+1 is viable, upgrade checker and rewrite tests with real match/no-match; (2) else shrink to single registration guard, delete zero-return assertions. |
| `tests/unit/aws_backup_related_test.go` | 22 | Single stub checker returning Count:0. Test validates registration and return value; no logic branches to break. |  | **verify + shrink or upgrade**: BackupPlansListMember lacks IAM role; GetBackupPlan returns full plan with rules incl. target vault — viable via N+1. Upgrade checker to resolve role ARN from GetBackupPlan, then rewrite test with found/not-found cases. Else shrink to registration guard. |
| `tests/unit/aws_codeartifact_related_test.go` | 22 | Single stub returning Count:0. Pure registration check plus trivial stub return. |  | **verify + shrink**: confirm CodeArtifact domain→kms cannot be resolved without DescribeDomain (viable via N+1). If upgradable, implement real checker. Else shrink to registration guard only. |
| `tests/unit/child_view_messages_test.go` | 28 | Purely struct field assertions — tests that setting a field results in the same field value. No behavior, no routing, no state transitions. | Almost entirely busywork; delete or merge into registry test. | **delete** — zero bug-catching value (testing that `x.Y = v` then reading `x.Y` returns `v`). Registry round-trip in `child_view_registry_test.go` covers any useful invariant. |
| `tests/unit/aws_docdb_snap_related_test.go` | 30 | Only tests registration and demo fixture — no actual checker logic tested (no found/not-found cases for dbc or kms matchers). |  | **expand**: add cache-driven tests — (a) matching dbc cluster present → Count≥1; (b) matching kms key present → Count≥1; (c) empty cache → Count=0 or -1 per truncation contract. Use existing ResourceCache pattern from `aws_ec2_related_cold_cache_live_test.go`. |
| `tests/unit/aws_ebrule_related_test.go` | 30 | Two registration-guard tests only (checker not nil, returns results). No logic paths, duplicate of infra-integrity coverage. | delete — redundant with demo_infrastructure_integrity Part4 | **delete** — fully redundant with `demo_infrastructure_integrity` Part4. |
| `tests/unit/aws_policy_related_test.go` | 30 | Mostly nil-client guard tests (`Count=-1`) for iam-user, iam-group, iam-role; no match/no-match tests via cache — these checkers likely require live IAM calls. | Primarily busywork nil-guards. | **rewrite with mock IAM client**: replace nil-client `Count=-1` checks with mocked `ListEntitiesForPolicy` returning (a) users+groups+roles attached; (b) empty; (c) API error. Nil-guards add no value. |
| `tests/unit/fixtures_test.go` | 30 | Test fixture data file — no test functions, only helper data for other tests. | Not a test file by itself; pure shared fixture data. | **keep (infra)** — fixture data consumed by other tests. Exclude from scoring. |
| `tests/unit/version_test.go` | 32 | Tests ResolveVersion for ldflags/dev/empty — low-risk buildinfo code, limited bug-catching value. | Low; version resolution is stable single-branch logic. | **keep as-is** — small, cheap regression guard for release-critical path (wrong version in `a9s --version` has bitten before). Do not expand, do not delete. |
| `tests/unit/aws_eb_related_test.go` | 35 | Checks that 3 related-defs are registered and demo checker returns results — pure registration/completeness guards with no logic tested. | delete — redundant with demo_infrastructure_integrity Part4 and demo_related_test | **delete** — fully covered by `demo_infrastructure_integrity` Part4 + `demo_related_test`. |
| `tests/unit/child_view_keys_test.go` | 35 | Four tests that each verify a single key binding matches a literal character — these are barely above trivially verifying that a constant equals itself. | Low value; key binding tests catch only typos. | **delete** — tests that literal characters equal themselves. Compile-time and README-diff checks cover this. |
| `tests/unit/demo_all66_test.go` | 38 | Single loop that checks every registered type has non-empty demo fixtures — already fully covered by demo_infrastructure_integrity Part1. | delete — fully redundant with TestDemoInfrastructureIntegrity Part1 | **delete** — 100% redundant with `demo_infrastructure_integrity` Part1. |
| `tests/unit/app_related_navigate_test.go` | 40 | Tests `FindResourceType` nil/non-nil and inline filter-text derivation logic — both are trivially testable registry lookups; the actual handler logic on the unexported `Model` is untested. | Real handler path untestable from unit tests; low value as compensating coverage. | **delete filter-text test + shrink**: `TestRelatedNavigate_FilterTextDerivation` is self-referential (replicates logic inline then tests that logic equals itself) — pure busywork, delete. Keep only the three FindResourceType guard tests. Open issue to add an integration test that exercises `handleRelatedNavigate` end-to-end once Model is exportable. |
| `tests/unit/demo_fixture_integrity_issue189_test.go` | 40 | Most subtests are either superseded by demo_infrastructure_integrity (Parts 1-4) or are lower-fidelity versions of the same checks. The cross-reference tests partially overlap but use different helpers. | Gap: largely superseded; consider merging unique cross-ref checks into infra_integrity | **keep (architect-revised 2026-04-08)**: `TestIssue189_RelatedDemo_IDsResolveToRealFixtures` uniquely extends the ID-resolution check to child types via `resource.AllChildTypes()` (lines 109-116) — infra_integrity Part 4 does NOT cover child types. `TestIssue189_DemoDependencies_CoreCrossReferences` also has unique cross-ref checks. Tests 1-2 could be dropped as duplicates of Parts 1-2 but the churn isn't worth it. |
| `tests/unit/demo_related_test.go` | 42 | Checks EC2 demo checker returns exactly 9 results and hardcoded counts (tg=1, asg=1, alarm=2, cfn=0, ebs≥1) — brittle count assertions will break on fixture changes, but no real logic path exercised. | Gap: counts will fail the moment fixture counts change; use presence+positive-count checks instead | **rewrite with presence-based assertions**: replace hardcoded count assertions (`tg.Count != 1`, `alarm.Count != 2`, etc.) with presence (`_, ok := byType["tg"]`) and positive-count (`Count > 0`) checks. Keep the 9-target assertion as a registration guard. |
| `tests/unit/filter_bench_test.go` | 42 | Benchmark for filter at 1000+ resources — validates perf contract but catches no logic bugs. | Low bug-catching value; benchmarks are informational only. | **keep as perf regression guard**: the 200ms threshold test IS load-bearing (filter is on the hot path; regression would be user-visible). Rename `TestFilterPerformance_Under200ms` to make intent clearer and move benchmarks behind `-bench` only. |
| `tests/unit/qa_view_interface_test.go` | 42 | Compile-time interface satisfaction checks plus FrameTitle non-empty guards; catches interface drift but most value is compile-time. | Redundant with compilation; runtime tests add marginal value. | **keep; score is too low**: this file covers stack push/pop, Filterable runtime dispatch, and SetSize propagation through the View interface — all real runtime invariants. The compile-time `_ views.View = ...` assertions are cheap; leave them. Rescore to ~62. |
| `tests/unit/aws_pipeline_related_test.go` | 45 | Returns constant `Count: 0` (undeterminable) for codebuild; tests only verify zero-count and demo smoke. | Low value — tests a stub, not real logic. | **upgrade checker then rewrite**: `GetPipeline` returns Role ARN and source/build stage configs (resolves CodeBuild project). This is a viable N+1 — not undeterminable. Coder: upgrade `checkPipelineCB`/`checkPipelineRole` to call GetPipeline. QA: rewrite tests with mock GetPipeline returning attached role + cb project, plus not-found and API-error cases. |
| `tests/unit/aws_r53_related_test.go` | 45 | Returns constant `Count: 0` for ELB and CloudFront; tests verify zero and demo smoke only. | Same as pipeline — tests a stub. | **upgrade checker then rewrite**: R53→ELB/CloudFront IS determinable via `ListResourceRecordSets` looking at `AliasTarget.DNSName` (suffix `.elb.amazonaws.com` or `.cloudfront.net`). Viable via N+1 per hosted zone. Coder: implement; QA: rewrite with cached RRSet fixtures containing alias targets, verify match and no-match. |
| `tests/integration/aws_test.go` | 48 | Integration tests with real AWS config; mostly skip in CI; limited bug-catching value in automated runs. | Low: almost always skipped in CI. | **keep (optional integration)**: do not run in CI; guard behind `-tags integration` and document in CONTRIBUTING.md. Value is as a local smoke when debugging AWS SDK upgrades. |
| `tests/unit/child_view_registry_test.go` | 48 | Mix of real registry behavior (register/get/unregister cycle) and trivial struct field checks (ChildViewDef fields equal what was set). | Struct field tests (TestChildViewDef_Fields, TestParentContext_Type) are noise; registry round-trips have real value. | **shrink**: delete `TestChildViewDef_Fields` and `TestParentContext_Type` (self-referential struct-field tests). Keep register/get/unregister round-trip tests. |
| `tests/unit/demo_child_fixtures_test.go` | 48 | Iterates 22 child types and asserts GetChildResources returns data; partially redundant with fixture_load's Part B, but catches missing registrations more explicitly. | Gap: no field-key validation on returned child resources | **expand**: for each child type, also assert returned resources have (a) non-empty ID, (b) required Fields keys from the child's ResourceTypeDef columns present, (c) RawStruct is the expected SDK type. Catches silent field-key drift. |
| `tests/unit/aws_cwlogs_test.go` | 52 | Only 3 tests — basic parse, error, empty. No nil-field, no StoredBytes formatting edge cases beyond the two shown, no pagination, no retention-days boundary. Thin relative to other aws_* files. | Gap: nil StoredBytes, nil RetentionInDays, nil CreationTime; StoredBytes byte-formatting (bytes/KB/GB boundaries) | **expand**: add (1) nil StoredBytes → "0" or "-"; (2) nil RetentionInDays → "Never" or equivalent; (3) nil CreationTime → empty; (4) byte formatting table: 0→"0 B", 1023→"1023 B", 1024→"1 KB", 1<<30→"1 GB", 1<<40→"1 TB"; (5) pagination multi-page fetch. |
| `tests/integration/clipboard_test.go` | 55 | Integration test for clipboard read/write; guarded by build tag, skips in headless CI; moderate value for clipboard correctness. | Low CI coverage due to skip conditions. | **keep (optional integration)**: valuable for local validation. Document in CONTRIBUTING that clipboard tests require a display/Wayland session. |
| `tests/unit/app_related_token_test.go` | 55 | Tests that a `RelatedNavigateMsg` with a truncated cache initiates a fetch (`cmd != nil`); cannot assert `Append=true` without real clients, so the token-resumption correctness is never verified. | Core behavior (Append=true vs false) untestable in this harness; integration test is needed. | **expose + expand**: add a seam to inject a fake fetch function (or export `handleRelatedNavigate` in an internal test helper) so the test can assert `Append=true` on truncated cache. Without this the test is a no-op guard. |
| `tests/unit/aws_ecs_services_test.go` | 55 | Three-step mock (list clusters → list services → describe), cluster ARN extraction table, empty/error cases. Moderate depth; missing nil-field guard and RawStruct assertion. | Gap: nil ServiceName/ClusterArn on DescribeServices response; RawStruct type check | **expand**: add nil-ServiceName, nil-ClusterArn, nil-Status, RawStruct is `ecstypes.Service` assertion. |
| `tests/unit/aws_profile_test.go` | 55 | Tests config-file parsing and missing-file grace; two of four tests are near-duplicates (`TestListProfiles_SampleFiles` vs `TestListProfiles_ConfigOnly`). | Duplicate test adds noise without new coverage. | **merge duplicates**: collapse `TestListProfiles_SampleFiles` and `TestListProfiles_ConfigOnly` into a table-driven test with each fixture as a row. Add malformed-INI error case. |
| `tests/unit/aws_sns_test.go` | 55 | Tests only field key existence (`topic_arn`, `display_name`), not exact values. Weak assertions miss mapping bugs where field key is right but value is wrong. | Exact-value assertions missing. | **rewrite with exact-value assertions**: replace `_, ok := fields["topic_arn"]` with `require.Equal("arn:aws:sns:...", fields["topic_arn"])`. Add error + empty cases. |
| `tests/unit/demo_no_real_webhooks_test.go` | 55 | Walks internal/demo/*.go for real Slack/Discord/PagerDuty webhook URLs; useful security guard but tests file content, not production logic. | Gap: add GitHub tokens, AWS secret patterns | **expand**: add patterns for (a) AWS access key IDs `AKIA[0-9A-Z]{16}`, (b) GitHub tokens `ghp_[A-Za-z0-9]{36}`, (c) bearer tokens `eyJ[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+`, (d) AWS secret keys `[0-9a-zA-Z/+]{40}` (with allowlist for fixture-base64). See `project_sensitive_data_cleanup.md` memory. |
| `tests/unit/demo_test.go` | 55 | Basic smoke tests (EC2/S3/Lambda/RDS fields, status variety, unknown type, S3Objects, YAML marshal, DemoConstants, EC2 naming variety) — partially superseded by infra_integrity and rawstruct tests. | Gap: largely redundant with other demo_* files; unique value is TestEC2NameVariety and TestDemoConstants | **shrink**: delete subtests superseded by `demo_infrastructure_integrity` and `demo_rawstruct_test`; keep only `TestEC2NameVariety` and `TestDemoConstants`. Rename file to `demo_ec2_naming_test.go`. |
| `tests/unit/qa_r53_records_config_test.go` | 55 | Verifies r53_records default config has correct column count, titles, widths, and detail paths; catches config-defaults drift. | Straightforward config completeness test — low surprise value. | **keep; generalize**: extract into a table-driven test that validates default config for ALL resource types (not just r53_records). This turns a one-off into a registry-wide contract guard. |
| `tests/unit/qa_registry_test.go` | 55 | Tests GetPaginatedFetcher nil/non-nil and register/unregister lifecycle; provides basic registry contract guard. | Non-nil guard tests have low bug-catch value; the mock-register test is more useful. | **shrink**: delete nil/non-nil guard tests; keep only the register/unregister lifecycle test. |
| `tests/unit/tui_styles_test.go` | 55 | Tests that style variables are initialized (non-nil foreground/background); guards against zero-value style regressions. | Low — initialization checks are low-risk. | **expand into NO_COLOR contract test**: test that `styles.Reinit()` under `NO_COLOR=1` produces ANSI-free output for every style; catches the NO_COLOR regression class that has bitten before. |
| `tests/unit/related_registry_test.go` | 58 | Tests RegisterRelated/GetRelated/RegisterNavigableFields roundtrips; somewhat overlaps with registry completeness tests. | Moderate value; basic registry store/retrieve is low-risk code. | **keep** — registry is load-bearing for all related-view dispatch. Round-trip coverage is cheap and correct. Do not delete. |
| `tests/integration/ec2_nav_chain_spec008_test.go` | 60 | Full EC2 navigation chain tests in integration package; require spec008 build tag and FieldCursor() getter; solid once the getter lands. | Currently can't compile without spec008 tag — limited current value. | **unblock or delete**: either land the `FieldCursor()` getter and enable the tag, or delete the file. Dormant integration tests that can't compile are dead weight. |
| `tests/unit/aws_ec2_related_cold_cache_live_test.go` | 60 | Tests the cold-cache (-1) path for live checkers that need API calls. Catches wrong default return when cache is empty. |  | **keep** — cold-cache default path is a real regression surface (issue #232). Solid as-is. |
| `tests/unit/aws_igw_related_test.go` | 60 | Mostly stub checkers (VPC association). Limited real logic. |  | **keep; score is too low**: scoring reasoning is wrong — this file tests real VPC and RTB checkers with found/not-found/cache-miss cases via RawStruct and cache. Rescore to ~68. |
| `tests/unit/demo_rawstruct_test.go` | 60 | Per-type (S3, Lambda, RDS) RawStruct type assertion + YAML marshal smoke tests for 3 types only; partially superseded by infra_integrity Part1/Part2 which covers all types. | Gap: YAML marshal tests only cover 3 of 66 types | **keep (architect-revised 2026-04-08)**: NOT subsumed by infra_integrity — this file does per-type SDK type-assertions (`s3types.Bucket`) and per-type invariants like `Bucket.Name == r.ID` which Part 1 does NOT check (Part 1 only checks `RawStruct != nil`). Original "delete" verdict was wrong. |
| `tests/unit/qa_profile_update_test.go` | 60 | Tests SelectorModel cursor movement, selection, and dismiss key behaviors; solid view state coverage for a simple view. | Nothing (tight for what it covers). | **keep** — tight, focused view-state coverage. No change. |
| `tests/unit/qa_ses_test.go` | 60 | Tests SES identity fetch parsing for domain vs email-address types; catches IdentityType mapping bugs. | Small scope; no error/empty case shown. | **expand**: add error path (API error) + empty list case. |
| `tests/unit/qa_sfn_test.go` | 60 | Tests SFN state machine fetch parsing including type enum (Standard vs Express); catches type mapping bugs. | Solid single-resource-type fetcher test. | **expand**: add error path + nil Name/Status guards. |
| `tests/unit/qa_trail_test.go` | 60 | Tests CloudTrail trail fetch field mapping including boolean fields and multi-region flag; catches bool-to-string mapping bugs. | Small scope; no error path shown. | **expand**: add error path + empty trails case. |
| `tests/unit/qa_waf_test.go` | 60 | Tests WAF WebACL fetch with field mapping and resource type definition; catches WAF-specific field presence bugs. | Small scope; no error path shown. | **expand**: add error path + empty WebACL list + nil Name/Scope guards. |
| `tests/integration/cli_test.go` | 62 | Integration tests for CLI binary (version flag, demo mode, invalid args); catches binary build and flag-handling regressions. | Nothing obvious — solid smoke coverage of the binary. | **keep** — cheap binary smoke. Add `--help` output stability test if flag set grows. |
| `tests/unit/aws_cloudformation_test.go` | 62 | Standard three-test fetcher: multi-resource parse, error, empty. Exact status-string assertions catch enum mapping bugs. |  | **expand**: add nil-StackStatus, nil-CreationTime guards; pagination NextToken path. |
| `tests/unit/aws_cloudwatch_test.go` | 62 | Standard three-test fetcher. Tests float threshold formatted as "80.00" — catches `%g` vs `%.2f` format bug. |  | **expand**: add threshold edge cases (0, negative, very large, NaN); nil-Threshold, nil-MetricName, nil-Dimensions guards; OK/ALARM/INSUFFICIENT_DATA state enum mapping. |
| `tests/unit/aws_docdb_test.go` | 62 | Standard fetcher tests. Tests instance-count extraction and endpoint mapping. |  | **expand**: add nil-Endpoint, nil-Port, nil-EngineVersion; empty instance list path; error path. |
| `tests/unit/aws_ebs_snap_related_test.go` | 62 | Similar pattern to other snap-related files — tests snapshot→volume and snapshot→kms ARN matchers. |  | **keep** — solid, matches the snap-related family pattern. |
| `tests/unit/aws_ebs_snap_test.go` | 62 | Standard three-test fetcher with snapshot-specific status enum assertions. |  | **expand**: add nil-VolumeSize, nil-StartTime, nil-Encrypted; pending/error status enum cases. |
| `tests/unit/aws_lambda_n1_test.go` | 62 | Tests Lambda N+1 fetch-by-ARN path. |  | **keep; score too low** — reasoning is wrong; this is the issue #221 N+1 regression guard using HTTP-level round-trip counting (5-function case included). Very high regression value. Rescore to ~80. |
| `tests/unit/aws_log_events_test.go` | 62 | Standard log-events child view tests. |  | **expand**: add (1) nil Message guard; (2) nil Timestamp guard; (3) pagination with NextForwardToken and idempotent forward-at-end; (4) RawStruct type assertion to `cwlogstypes.OutputLogEvent`. |
| `tests/unit/aws_log_streams_test.go` | 62 | Standard log-streams child view tests. |  | **expand**: add nil LastEventTimestamp, nil StoredBytes guards; descending=true ordering assertion; pagination. |
| `tests/unit/demo_fixture_load_test.go` | 62 | panic-safety sweep over all demo fetchers + ParseTime error-return TDD test (currently a compile-time contract enforcer for a coder task) — the panic sweep catches fixture construction bugs. | Gap: ParseTime test only runs once coder exports the function; currently tests compile with demo.ParseTime | **keep + land ParseTime export**: dispatch coder to export `demo.ParseTime` so the error-return test actually runs. Until then the panic-sweep Part is load-bearing on its own. |
| `tests/unit/demo_render_test.go` | 62 | E2E render test through real views.yaml config, checking 4 resource types for specific strings in rendered list output — catches view-config/field-mapping mismatches that unit tests miss. | Gap: only 4 of 66 types tested; no size-variant testing | **expand to all resource types**: convert to a table-driven sweep over `resource.AllResourceTypes()` — for each type, render list view against demo fixtures and assert (a) non-empty, (b) no "<nil>", (c) contains at least one fixture ID. Adds multi-size (80x24, 120x40, 200x60) sweep. |
| `tests/unit/qa67_demo_test.go` | 62 | Verifies demo fixtures have at least one error-state resource (needed for row-coloring coverage) and that profile/sort/filter work in demo mode; real value since demo mode is a distinct code path. | gap: G.8 sort/filter tests only check non-empty View(), not correctness of sorted order | **expand**: replace G.8's "non-empty View()" assertions with actual sort-order verification — parse the rendered list and assert rows are in Name/Status order. Add a filter-applied assertion that filtered-out rows are absent from output. |
| `tests/unit/qa_pipeline_test.go` | 62 | Tests CodePipeline fetch mapping including PipelineType and ExecutionMode enum fields, plus error/empty cases; catches enum-to-string mapping bugs. | Solid single-resource-type fetcher test; limited in scope. | **keep** — solid; limited scope is intentional (one resource type per file). |
| `tests/unit/qa_rds_snapshots_test.go` | 62 | Tests RDS snapshot fetcher with field mapping and type definition checks; catches snapshot-type and status field mapping bugs. | Solid single-resource-type fetcher test; limited scope. | **keep**. |
| `tests/unit/qa_redshift_test.go` | 62 | Tests Redshift cluster fetch parsing including nil-safe endpoint/port handling; catches nil-pointer bugs on clusters without endpoints. | Small scope; no error path tested. | **expand**: add error path + empty cluster list. |
| `tests/unit/qa_reveal_test.go` | 62 | Tests RevealModel view/before-size, FrameTitle, CopyContent, key bindings, and clipboard copy; covers the full reveal view API surface. | Solid; clipboard copy test may be platform-fragile. | **keep + guard clipboard test**: wrap the clipboard subtest in `if clipboard.Unsupported { t.Skip(...) }` so headless CI doesn't fail intermittently. |
| `tests/unit/qa_sns_subscriptions_test.go` | 62 | Tests SNS subscription fetch with topic name extraction and field mapping; catches ARN-parsing bugs for name derivation. | Limited to happy path shown; error cases covered elsewhere. | **expand**: add malformed-ARN case (no colons, wrong service, empty). |
| `tests/unit/qa_transit_gateways_test.go` | 62 | Tests TGW fetch with tag-based name extraction and missing-tag fallback; catches tag-name extraction regression. | Missing tag fallback is the key case; rest is standard. | **keep**. |
| `tests/unit/qa_vpc_endpoints_test.go` | 62 | Tests VPCE fetch with gateway vs interface endpoint type mapping; catches VpcEndpointType enum-to-string bugs. | Standard fetcher test; limited scope. | **expand**: add GatewayLoadBalancer endpoint type + error + empty cases. |
| `tests/unit/s3_perf_test.go` | 62 | Tests FetchS3Buckets uses no GetBucketLocation calls (perf contract); catches accidental re-introduction of per-bucket API calls. | Moderate; perf is a contract, not a correctness test. | **keep** — round-trip-count perf contracts are high-value regression guards (same class as `aws_lambda_n1_test.go`). |
| `tests/unit/aws_ecs_clusters_test.go` | 63 | Two-step list+describe pattern tests including status and task-count field extraction. |  | **expand**: add nil ClusterName, nil Status, nil ActiveServicesCount; error on DescribeClusters; empty cluster list. |
| `tests/unit/aws_elb_listeners_test.go` | 63 | Tests listener port/protocol field extraction. |  | **expand**: add nil Port, nil Protocol, HTTP/HTTPS/TCP/TLS/GENEVE enum coverage, cert ARN on HTTPS. |
| `tests/unit/aws_iam_groups_related_test.go` | 63 | Registration and demo fixture tests for IAM groups. Partial coverage. |  | **expand**: add real cache-driven group→policy and group→user matchers (found, not-found, cache-miss). |
| `tests/unit/aws_iam_users_related_test.go` | 63 | Registration and demo fixture for IAM users. |  | **expand**: add user→group (from RawStruct Groups), user→policy (attached), user→access-key checker tests with real cache fixtures. |
| `tests/unit/aws_igw_test.go` | 63 | Standard fetcher with attachment-state and VPC-ID field assertions. |  | **expand**: add nil Attachments (detached IGW), nil VpcId, multi-attachment case (rare but possible), error path. |
| `tests/unit/aws_lambda_code_test.go` | 63 | Tests lambda code-detail child view fetcher. |  | **expand**: add nil CodeSha256, nil CodeSize, Zip vs Image PackageType handling, error path. |
| `tests/unit/fieldpath_fieldlist_test.go` | 63 | Tests ExtractFieldList including nested struct expansion and navigable-field mapping — catches field-list regression bugs. | Nothing obvious. | **keep**. |
| `tests/unit/qa_detail_layout_test.go` | 63 | Checks every key-value line has a colon separator — catches format regression but is brittle to intentional layout changes. | gap: soft assertions (skips lines starting with "-", "Code:", "Name:") | **rewrite**: replace "every line has a colon" with specific assertions: (1) each detail path from views.yaml appears exactly once; (2) values are right-aligned to a common column; (3) empty values render as "-" not "". Less brittle, catches more. |
| `tests/unit/qa_yaml_child_views_test.go` | 63 | Tests YAML view for log streams and log events child resource types; catches YAML rendering regression for child view resources. | Narrow to log types; fixture is minimal. | **expand**: add YAML rendering for ALL child view types (see child-view registry). Convert to table-driven sweep. |
| `tests/unit/qa_yaml_ec2_family_test.go` | 63 | Tests YAML view for EC2-family resources (VPC, SG, Node Groups etc.) with field presence assertions; catches YAML rendering regression. | Same presence-only pattern; covers useful resource breadth. | **merge** with `qa_yaml_services_test.go` and `qa_yaml_v220_test.go` into a single table-driven `qa_yaml_all_test.go` over `resource.AllResourceTypes()`. Rewrite presence-only assertions into (a) parses as valid YAML, (b) top-level keys match ResourceTypeDef columns, (c) no "<nil>" in output. |
| `tests/unit/qa_yaml_services_test.go` | 63 | Tests YAML view for Lambda, Alarms, and other service resources; catches YAML rendering regression for service-layer resources. | Presence-only assertions; useful breadth coverage. | **merge** — see `qa_yaml_ec2_family_test.go` action. |
| `tests/unit/qa_yaml_v220_test.go` | 63 | Tests YAML view for v2.2.0 resources (CloudFront, R53, API GW) including CloudTrail event fixture; catches YAML rendering regression for newer resource types. | Good that it covers newer types; presence-only assertions. | **merge** — see `qa_yaml_ec2_family_test.go` action. |
| `tests/unit/aws_autoscaling_test.go` | 65 | Tests nil-status vs string-status ASG distinction, instance count extraction, exact numeric field stringification. Catches format bugs in min/max/desired/instances int→string mapping. |  | **keep**. |
| `tests/unit/aws_cfn_related_test.go` | 65 | Single ARN-last-segment role match checker with nil-RoleARN and nil-cache cases. Solid for one checker but limited breadth. |  | **expand**: add CFN→resources-in-stack (S3, EC2, RDS, etc.) via ListStackResources stubs so we actually cover the high-value related links. |
| `tests/unit/aws_ct_events_test.go` | 65 | Tests basic CloudTrail event parsing, paging, field presence, and initial status assignment. Good baseline coverage but superseded by the redesign tests. |  | **shrink**: delete subtests that are fully covered by `aws_ct_events_redesign_test.go` / `_severity_test` / `_verb_classification_test`. Keep only baseline paging happy path as a smoke test. |
| `tests/unit/aws_ebs_test.go` | 65 | Standard fetcher plus EBS-specific attachment-count and encryption field tests. |  | **keep**. |
| `tests/unit/aws_eip_related_test.go` | 65 | Tests ENI and EC2 instance association lookups via AllocationId. |  | **keep**. |
| `tests/unit/aws_eip_test.go` | 65 | Tests EIP association state logic (associated vs unassociated) and exact field mapping. |  | **keep**. |
| `tests/unit/aws_eks_test.go` | 65 | Two-step EKS cluster fetch tests with status and version field assertions. |  | **expand**: add nil Endpoint, nil RoleArn, nil Version, PLATFORM_VERSION field. |
| `tests/unit/aws_elb_listener_rules_test.go` | 65 | Tests child-view listener-rules fetching and field mapping. |  | **keep**. |
| `tests/unit/aws_elb_test.go` | 65 | Standard multi-LB parse tests with type/scheme/state field assertions. |  | **keep**. |
| `tests/unit/aws_glue_related_test.go` | 65 | Tests S3 and KMS related checkers for Glue. |  | **keep**. |
| `tests/unit/aws_iam_group_related_test.go` | 65 | Tests IAM group→policy and group→role checkers via RawStruct patterns. |  | **keep**. |
| `tests/unit/aws_iam_policies_related_test.go` | 65 | Tests policy→role and policy→user attachment checkers. |  | **expand**: add real policy→group attachment (currently nil-guard only — see `aws_policy_related_test.go`). |
| `tests/unit/aws_iam_roles_test.go` | 65 | Standard IAM role fetcher tests with path and assume-role-policy field assertions. |  | **expand**: add nil AssumeRolePolicyDocument, URL-encoded JSON doc edge case, error path. |
| `tests/unit/aws_iam_user_related_test.go` | 65 | Tests IAM user→group membership checker. |  | **expand**: add user→access-key and user→policy checkers (attached + inline). |
| `tests/unit/aws_lambda_test.go` | 65 | Standard lambda fetcher with runtime, arch, and role field assertions. |  | **expand**: add Image PackageType path, nil Layers, nil DeadLetterConfig. |
| `tests/unit/aws_sns_sub_related_test.go` | 65 | Tests SNS topic ARN exact match and no-match; simple single-field lookup. | Low complexity; could add pending-subscription edge case. | **expand**: add pending-subscription (`PendingConfirmation` as SubscriptionArn literal) and `Deleted` subscription filter. |
| `tests/unit/aws_toplevel_pagination_batch5_test.go` | 65 | ECS and Kinesis pagination only; thin compared to other batches. | ECS ListClusters pagination is noted as a known bug path — value is high but file is sparse. | **expand**: add (a) ECS multi-page list+describe flow ensuring NextToken correctly propagates through both steps; (b) Kinesis ExclusiveStartStreamName pagination; (c) error midway through pagination leaves partial results handled correctly. |
| `tests/unit/aws_waf_related_test.go` | 65 | Tests nil-client guard only; no real ELB association match/no-match tests because WAF→ELB requires ListResourcesForWebACL which needs live clients. | Real match/no-match logic untested. | **rewrite with mock WAFv2 client**: inject a `WAFv2ListResourcesForWebACLAPI` interface and mock it. Add found/not-found/error cases. Delete nil-client guards. |
| `tests/unit/bug_reveal_related_column_narrow_width_test.go` | 65 | Width boundary test (76 cols); same class as medium-width but a different threshold value — useful if the breakpoint is a constant that might drift. | Partially redundant with medium/resize variants. | **merge** with `bug_reveal_related_column_medium_width_test.go` into a single table-driven `bug_reveal_related_column_widths_test.go` with width rows [40, 76, 95, 120, 160] and expected visibility per row. |
| `tests/unit/connect_aws_region_test.go` | 65 | Covers the "Missing Region" regression path for profile switching and region selection; tests are necessarily shallow (isolated env, no real SDK calls) but guard the fallback resolution logic. | Several tests skip/log-only rather than hard-fail, reducing bug-catching reliability. | **rewrite soft assertions**: replace `t.Skipf`/`t.Logf` with `t.Errorf` where the code path IS testable in an isolated env. Only skip tests that genuinely need AWS SDK calls. |
| `tests/unit/demo_ec2_fixtures_spec008_test.go` | 65 | 5 "FAILS NOW" tests that act as TDD drivers for missing fixture fields (ImageId, KeyName, Architecture, Placement, SecurityGroups), plus regression guards for existing VpcId/SubnetId cross-refs — these are real bug-catching tests for fixture gaps. | Nothing; intentionally failing tests are load-bearing | **keep + complete**: dispatch coder to add the 5 missing fields to EC2 fixtures so the "FAILS NOW" tests flip to PASS. Once green, this becomes a permanent fixture-completeness regression guard. Rescore to ~78. |
| `tests/unit/fieldpath_enumerate_test.go` | 65 | Tests EnumeratePaths for flat/nested/slice/pointer/time structs — catches path enumeration bugs. | Nothing obvious. | **keep**. |
| `tests/unit/fieldpath_format_test.go` | 65 | Tests FormatValue for time, pointers, named string types, nil cases — catches formatting regressions in the reflection layer. | Nothing obvious. | **keep**. |
| `tests/unit/layout_frame_test.go` | 65 | Tests PadOrTrunc, frame helpers; catches truncation and padding bugs in the layout layer. | Nothing obvious. | **keep**. |
| `tests/unit/qa_copy_test.go` | 65 | Copy-to-clipboard in list and detail views; tests both success flash content and skips when clipboard unavailable — solid but clipboard path is environment-dependent. | gap: partial read; skips on most CI environments | **extract copy logic**: split the function under test so the "what to copy" logic (returns a string) is tested without clipboard, and only the final syscall is behind the skip. Currently most of the file is dead in CI. |
| `tests/unit/qa_profile_switch_test.go` | 65 | Tests that ProfileSelectedMsg triggers reconnect and subsequent ClientsReadyMsg triggers refresh; catches profile-switch-leaves-stale-data bug. | Assertion is soft ("or Connected") — could be tightened. | **tighten assertions**: replace `or Connected` disjunction with an ordered state-machine check: after ProfileSelectedMsg → status is "Reconnecting"; after ClientsReadyMsg → status is "Connected" AND resource cache is cleared. |
| `tests/unit/qa_redis_test.go` | 65 | Tests Redis list view rendering with multi-status color verification; catches status color regression for redis-specific statuses. | Limited to view rendering; no fetch-layer test in this file. | **keep** — fetch layer is covered by `aws_redis_test.go` (if present) or should be added there, not here. |
| `tests/unit/qa_s3_object_detail_test.go` | 65 | Tests fieldpath extraction for s3types.Object and CommonPrefix via config detail paths; catches fieldpath panic on AWS SDK struct shapes. | Panic-via-defer pattern is correct; narrow to two S3 sub-types. | **keep**. |
| `tests/unit/qa_view_methods_test.go` | 65 | Tests CopyContent across all view types (empty/loaded/detail) and FrameTitle behavior; catches CopyContent regression when new views are added. | Good cross-view coverage; relies on correct content value assertions. | **keep**. |
| `tests/unit/qa_yaml_test.go` | 65 | Tests YAML view rendering for all core resource types (S3, EC2, RDS, Redis, DocDB, EKS, Secrets) with field presence assertions. | Broad resource type coverage; assertions are presence-only not value-exact. | **merge** into consolidated `qa_yaml_all_test.go` (see `qa_yaml_ec2_family_test.go` action) — table-driven over all resource types with parseable-YAML + no-<nil> assertions. |
| `tests/unit/resource_pagination_test.go` | 65 | Tests RegisterPaginated/GetPaginated roundtrip and FetchResult; basic registry correctness. | Moderate; low-risk code path. | **keep** — registry is load-bearing. |
| `tests/unit/scroll_state_test.go` | 65 | Tests ScrollState cursor, total, boundary clamping; basic state machine correctness. | Nothing obvious — straightforward state. | **keep**. |
| `tests/unit/bug_reveal_related_column_medium_width_test.go` | 66 | Width boundary test (95 cols) for RELATED panel visibility; catches threshold regressions in the auto-show logic. | Partially redundant with narrow/resize tests. | **merge** into `bug_reveal_related_column_widths_test.go` (see narrow-width action). |
| `tests/unit/qa_availability_test.go` | 66 | Tests availability probe display in main menu across all resource types with multiple states (dim/bright); catches availability coloring regressions. | gap: file exceeds token limit — could not read past line 100; scoring on partial view | **rescore after full read**; if breadth across all resource types is confirmed, **keep** — otherwise table-drive the state matrix to tighten. |
| `tests/unit/qa_coverage_gaps_test.go` | 66 | PropagateSize across all 4 stacked views; tests that resize actually changes output — catches broken resize propagation. | gap: partial read — scored on first 60 lines | **keep** — SetSize propagation is a known regression class. No change. |
| `tests/unit/qa_eks_secrets_test.go` | 66 | EKS cluster list columns; notes RawStruct limitation for config-driven columns — similar to EC2 pattern. | gap: partial read | **keep**. |
| `tests/unit/qa_help_profile_region_test.go` | 66 | Help screen opens from menu/list, profile/region shown in header — basic functional regression guards. | gap: partial read; soft string-contains assertions only | **tighten assertions**: replace soft `strings.Contains(out, "default")` with explicit segment extraction from the header row (e.g. parse "profile: default" by regex and assert equality). |
| `tests/unit/qa_iam_groups_test.go` | 66 | IAM group fetch — simpler than users, no optional field edge case. | gap: standard pattern | **expand**: add error path + empty group list + group with zero users. |
| `tests/unit/qa_glue_test.go` | 67 | Glue Jobs mapping: worker type, version, number-of-workers — standard pattern with exact assertions. | gap: standard fetcher test | **expand**: add error + empty + nil-WorkerType + Python Shell job type. |
| `tests/unit/qa_kinesis_test.go` | 67 | Kinesis stream mapping with StreamStatus enum — standard fetcher pattern. | gap: standard fetcher test | **expand**: add error + empty + all StreamStatus enum values (CREATING/DELETING/ACTIVE/UPDATING). |
| `tests/unit/qa_pagination_security_test.go` | 67 | Tests paginated sg/iam/secrets/ssm fetchers with error paths and multi-page flows; catches IAM Marker vs ContinuationToken distinctions. | Narrow fixture values; no large-count (1000+) boundary test. | **expand**: add 1000+ item boundary test verifying pagination terminates and memory stays bounded. |
| `tests/unit/aws_ami_related_test.go` | 68 | Covers Pattern C (ec2 cache) and Pattern F (EBS snap from RawStruct), including invalid-RawStruct guard. Multiple realistic paths tested with exact ResourceIDs assertions. |  | **keep**. |
| `tests/unit/aws_ct_events_related_test.go` | 68 | Tests IAMUser/role username-field matching via cache patterns. Real checker logic tested with found/not-found/cache-miss cases. |  | **keep**. |
| `tests/unit/aws_ddb_related_test.go` | 68 | Tests KMS ARN match, DDB-stream lambda trigger (RawStruct-based), and dimension alarm match. Typical solid related-checker coverage. |  | **keep**. |
| `tests/unit/aws_dynamodb_test.go` | 68 | Tests two-step list+describe pattern with per-table field assertions. Catches mapping bugs in the describe→fields pipeline. |  | **keep**. |
| `tests/unit/aws_ec2_test.go` | 68 | Tests multi-reservation parsing, tag-based name extraction, and status enum mapping. Catches name-extraction bugs specific to EC2's nested structure. |  | **keep**. |
| `tests/unit/aws_ecs_svc_events_test.go` | 68 | Field mapping, nil fields, newline stripping, RawStruct, column verification, child registration, parent child-def assertions — good overall coverage. | Gap: no test for the 100-event truncation behavior documented in some ecs_svc_events implementations | **expand**: add 150-event input, assert output truncated to 100 with most-recent-first ordering preserved. |
| `tests/unit/aws_ecs_svc_logs_test.go` | 68 | (File exceeded read limit but structure mirrors other svc child tests.) Based on pattern: field mapping, nil, stripping, pagination, RawStruct, registration. | Gap: status classification for CloudWatch log lines (ERROR/WARN/INFO) | **expand**: add log-line status classification table — "ERROR ..." → Error, "WARN ..." → Warning, "INFO ..." → Info, lowercase variants, log-level-like prefixes (`[ERROR]`, `level=error`). |
| `tests/unit/aws_ecs_tasks_related_test.go` | 68 | Tests Group-field service extraction and cluster-name extraction from ECS Task RawStruct. |  | **keep**. |
| `tests/unit/aws_efs_related_test.go` | 68 | Tests navigable KmsKeyId field and CFN/subnet/sg related checkers. |  | **keep**. |
| `tests/unit/aws_errors_test.go` | 68 | Tests error classification routing for token-expiry, throttle, access-denied, and unknown error codes. Catches misclassification in the error-routing switch. |  | **keep**. |
| `tests/unit/aws_glue_runs_test.go` | 68 | (File exceeded.) Pattern consistent with other glue tests: field mapping, nil, timestamp, pagination, RawStruct — solid standard coverage. | Gap: duration computation for runs without EndTime | **expand**: add run-in-progress case (EndTime nil) — duration should be "running" or computed vs now, not "-1" or negative. |
| `tests/unit/aws_iam_roles_related_test.go` | 68 | Tests role→policy attachment and role→lambda/ecs trust checker via ARN parsing. |  | **keep**. |
| `tests/unit/aws_kinesis_related_test.go` | 68 | Tests StreamName dimension alarm match and navigable-field absence assertion. |  | **keep**. |
| `tests/unit/aws_logs_related_test.go` | 68 | Tests log-group→lambda name parsing and alarm dimension match. Catches the `/aws/lambda/{name}` parse regex. |  | **keep**. |
| `tests/unit/aws_msk_related_test.go` | 68 | Tests Pattern C alarm dimension matching, navigable-field absence, and demo fixture smoke check — realistic mapping bugs. | Nothing obvious; could add a "wrong dimension key" negative case. | **expand**: add wrong-dimension-key negative case (Cluster="..." instead of ClusterName="...") → expect Count=0. |
| `tests/unit/aws_opensearch_related_test.go` | 68 | Tests CW alarm dimension match/no-match for `DomainName` — same pattern as MSK/Redis/Redshift. | Nothing notable. | **keep**. |
| `tests/unit/aws_redis_related_test.go` | 68 | Tests CW alarm CacheClusterId dimension match/no-match; navigable-field absence verified. | Nothing notable. | **keep**. |
| `tests/unit/aws_redshift_related_test.go` | 68 | Tests CW alarm ClusterIdentifier dimension match/no-match; same pattern as redis/opensearch. | Nothing notable. | **keep**. |
| `tests/unit/aws_routetables_test.go` | 68 | Tests route counts, main-association flag, empty associations path, and tag extraction. | Missing nil VpcId guard. | **expand**: add nil VpcId, nil RouteTableId, nil PropagatingVgws guards. |
| `tests/unit/aws_secrets_test.go` | 68 | Tests rotation enabled/disabled, LastAccessed/LastChanged date formatting, and description mapping. | Missing nil-date guard. | **expand**: add nil LastAccessedDate, nil LastChangedDate, nil RotationRules guards. |
| `tests/unit/aws_ses_related_test.go` | 68 | Tests R53 hosted-zone domain match using trailing-dot normalization — the normalization logic is regression-prone. | Nothing obvious; normalization test is the key value here. | **keep**. |
| `tests/unit/aws_sfn_related_test.go` | 68 | Tests log-group naming-convention match for SFN, CW alarm StateMachineArn dimension match, no-match paths. | Nothing notable. | **keep**. |
| `tests/unit/aws_ssm_related_test.go` | 68 | Tests KMS key match for SecureString, non-SecureString returns 0, no-match path. | Nothing notable. | **keep**. |
| `tests/unit/aws_ssm_test.go` | 68 | Tests SecureString vs plain String types, version, last-modified date, KMS key ID, and tier fields. | Missing error case and nil LastModifiedDate guard. | **expand**: add error path + nil LastModifiedDate + nil KmsKeyId + Advanced/Intelligent-Tiering tier enum. |
| `tests/unit/config_split_test.go` | 68 | ParseSingle and LoadFromDirs tests covering YAML parsing, layer merge, empty/invalid dirs, non-YAML skipping — solid coverage of edge cases in the config loading pipeline. | The key-field test is partially redundant with config_test.go. | **shrink**: delete the duplicate key-field test (already in `config_test.go`). Keep the split-file pipeline tests. |
| `tests/unit/ctdetail_summarize_iam_test.go` | 68 | CreateRole residual fields and DeleteRole empty-params path; the presence checks are real but only 2 events tested. | Limited event coverage compared to the full IAM event taxonomy. | **expand**: add AttachRolePolicy, PutRolePolicy, UpdateAssumeRolePolicy, CreateUser, CreateAccessKey, DeleteAccessKey events (the high-frequency IAM audit events). |
| `tests/unit/demo_transport_test.go` | 68 | Exercises the demo HTTP transport by making real SDK calls through it for all 40+ AWS services — catches transport routing misses that no other test covers. Based on visible header it creates per-service SDK clients. | Gap: no assertion on response data contents beyond non-empty; no error-path test for unsupported operations | **expand**: (1) for each service add one exact-value assertion on a representative field; (2) add error-path test for an unsupported operation → expect transport returns a well-formed AWS error response, not a panic or 500. |
| `tests/unit/ec2_fixture_crossref_test.go` | 68 | Cross-references demo fixture IDs against registered fetchers and related-demo checkers; catches mismatches that cause silent navigation failures in demo mode. | Nothing obvious. | **keep + generalize**: extend the cross-ref sweep from EC2-only to ALL resource types that have navigable fields. Each RawStruct navigable field value must resolve to a fixture ID in the target type. |
| `tests/unit/fieldpath_extract_test.go` | 68 | Tests ExtractSubtree with nested structs, slices, pointer deref, nil fields — the core extraction logic for detail views. | Nothing obvious. | **keep**. |
| `tests/unit/first_screen_journey_regression_test.go` | 68 | Regression guard for EC2 list → Enter → detail showing related column; catches wiring regressions in the first-screen path. | Narrow: single resource type, single path. | **expand**: add the same journey for 3-4 other resource types (VPC, RDS, Lambda, S3) to catch resource-type-specific wiring regressions. |
| `tests/unit/layout_twocolumn_test.go` | 68 | Tests TwoColumn layout for total width, left/right panel widths, separator; catches off-by-one width regressions. | Nothing obvious. | **keep**. |
| `tests/unit/qa67_concurrency_test.go` | 68 | State-machine sequences (resource deleted before detail, late ResourcesLoadedMsg after Esc, rapid Ctrl+R) exercise non-trivial message ordering that could corrupt app state; non-trivial realistic regression coverage. | gap: soft assertions in several tests (t.Logf instead of t.Errorf) reduce catch rate | **tighten assertions**: convert every `t.Logf` into `t.Errorf` where the expected post-condition is known. If a post-condition is genuinely non-deterministic, document WHY in a comment and remove the assertion entirely. No "pass-regardless" logs. |
| `tests/unit/qa67_view_errors_test.go` | 68 | Reveal/error message handling, x-key no-op across 8 non-secret types, sort on no-status-column type, horizontal scroll boundary — all realistic paths that could regress. | gap: I.4 "Secret visible" and I.7 tab autocomplete both use t.Logf soft checks | **tighten I.4 and I.7**: I.4 should assert secret value present in View() after Reveal. I.7 should assert completion list contains expected profiles. If assertable, use Errorf; else delete. |
| `tests/unit/qa_athena_test.go` | 68 | Exact field-value assertions (workgroup_name, state, engine_version), nil EngineVersion path tested separately — these catch real mapping bugs. | gap: no ResourceTypeDef column registration test | **keep**; column registration covered by `column_key_mismatch_test.go` sweep. |
| `tests/unit/qa_codeartifact_test.go` | 68 | Standard fetcher mapping test with exact field assertions; catches repo_name/domain_name mapping errors. | gap: only 2 resources tested, no nil-owner guard | **expand**: add nil DomainOwner + error path. |
| `tests/unit/qa_detail_ec2_family_test.go` | 68 | Custom detailConfigForType builder for networking types (subnet, rtb, nat, igw, eip) not yet in defaults.go — preparatory scaffold for new types. | gap: tests for types not yet registered may trivially skip | **audit + remove scaffold**: check which types are now in defaults.go; delete scaffold for those types and migrate assertions to `qa_detail_paths_test.go`. If any types are still missing from defaults.go, dispatch coder to add them. |
| `tests/unit/qa_eb_test.go` | 68 | Elastic Beanstalk environments (Ready/Updating), health coloring (Green/Yellow) — catches status/health field mapping. | gap: standard fetcher test | **expand**: add Red/Grey health states + error path. |
| `tests/unit/qa_ebrule_test.go` | 68 | EventBridge rule mapping: schedule vs event-pattern fields, enabled/disabled states — catches field routing bugs between the two rule types. | gap: partial read | **keep** — the schedule-vs-event-pattern routing is the key value. |
| `tests/unit/qa_ecr_test.go` | 68 | ECR repository fetch with scan-on-push flag and immutability setting — catches boolean field mapping. | gap: standard pattern | **expand**: add error path + empty list + nil EncryptionConfiguration. |
| `tests/unit/qa_eni_test.go` | 68 | ENI fetch with status/interface-type mapping and Tag extraction; tests both in-use and available states. | gap: standard pattern, nothing unusual | **keep**. |
| `tests/unit/qa_help_context_test.go` | 68 | Context-aware help: main menu help shows correct key list — catches help content completeness regressions. | gap: partial read | **expand**: add help contexts for detail, yaml, list, selector views — ensure each shows the correct key list for that context. |
| `tests/unit/qa_iam_policies_test.go` | 68 | IAM policy fetch with attachment count and path field — standard pattern. | gap: standard fetcher test | **keep**. |
| `tests/unit/qa_iam_users_test.go` | 68 | IAM user fetch with PasswordLastUsed nil/non-nil distinction — catches optional field handling. | gap: standard pattern | **keep**. |
| `tests/unit/qa_mainmenu_test.go` | 68 | All resource types visible, aliases shown in menu — basic completeness regression guard. | gap: tests are shallow string-contains checks | **tighten**: iterate over `resource.AllResourceTypes()` and for each type assert the line "<ShortName> <Name> <alias1> <alias2>" appears exactly once in the rendered menu. |
| `tests/unit/qa_msk_test.go` | 68 | MSK cluster v2 list API with Provisioned/Serverless cluster types — catches cluster_type field mapping. | gap: standard fetcher test | **keep**. |
| `tests/unit/qa_opensearch_test.go` | 68 | Tests the two-step ListDomainNames→DescribeDomains fetch flow which is uniquely error-prone; verifies field mapping and both error paths. | Missing DescribeError case for nil resources check could be tighter. | **expand**: add nil-resources-on-DescribeError (currently allows either nil or empty; pin to one). |
| `tests/unit/qa_pagination_monitoring_test.go` | 68 | Tests paginated alarm/logs/ddb/sqs/sns fetchers with error paths; catches NextPageToken vs NextToken naming bugs per-service. | No multi-page append test — only first-page is verified in the excerpt. | **expand**: for each fetcher add a 2-page test where page-1 returns a token, page-2 returns empty, and verify (a) combined results have both pages merged, (b) the exact token received from page-1 is passed as input to page-2. |
| `tests/unit/qa_pagination_remaining_test.go` | 68 | Tests 11 remaining fetchers (eventbridge, kinesis, msk, sfn, ses, waf, etc.) covering distinct pagination styles; catches token field name divergence across SDK clients. | Same gap: token forwarding not asserted as input to second call. | **expand**: same as `qa_pagination_monitoring_test.go` — add input-token assertions for each fetcher's page-2 call. |
| `tests/unit/qa_rds_test.go` | 68 | Tests RDS list view rendering including status colors and config-driven columns; catches style regression and field-display bugs. | Needs broader multi-status color coverage per the description. | **expand**: add fixtures and assertions for available/creating/stopped/modifying/failed status colors. |
| `tests/unit/reveal_registry_test.go` | 68 | Tests RegisterRevealFetcher/GetRevealFetcher; some tests FAIL until coder adds the registry functions; documents the API contract. | Nothing obvious. | **keep + land registry**: dispatch coder to add the registry functions so the failing tests flip to PASS. |
| `tests/unit/tui_content_views_test.go` | 68 | Tests MainMenu rendering completeness, YAML view, detail view basic output; catches content view wiring. | Nothing obvious. | **keep**. |
| `tests/unit/tui_identity_test.go` | 68 | Tests identity view content, account section rendering, ARN display; catches identity view regressions. | Nothing obvious. | **keep**. |
| `tests/unit/tui_selector_test.go` | 68 | Tests profile/region selector construction, key navigation, selection; catches selector wiring regressions. | Nothing obvious. | **keep**. |
| `tests/unit/tui_wiring_test.go` | 68 | Tests clipboard copy flash message wiring in resource list; catches copy-wiring regressions. | Nothing obvious. | **keep**. |
| `tests/unit/qa_efs_test.go` | 69 | EFS file system with size, mount targets, tags, lifecycle state — catches numeric and nested field extraction. | gap: standard pattern | **expand**: add error path + empty list + nil SizeInBytes. |
| `tests/unit/aws_acm_test.go` | 70 | Solid field-mapping coverage with exact-value assertions across multiple status/type enums. Catches wrong string conversions for `ISSUED`, `PENDING_VALIDATION`, `AMAZON_ISSUED`, `IMPORTED`, bool fields. |  | **keep**. |
| `tests/unit/aws_apigw_related_test.go` | 70 | Tests two distinct log-group naming patterns (execution-logs by ID, access-logs by name), no-match, and nil-cache. Catches pattern-matching bugs in a dual-pattern lookup. |  | **keep**. |
| `tests/unit/aws_ebs_related_test.go` | 70 | Tests navigable-field registration and KMS ARN matching via RawStruct `KmsKeyId` field. Catches navigable-field wiring bugs. |  | **keep**. |
| `tests/unit/aws_ecr_related_test.go` | 70 | Tests navigable-field registration (RepositoryUri→lambda) and Lambda/CFN/CB use-of-ECR checkers via RawStruct patterns. |  | **keep**. |
| `tests/unit/aws_ecs_services_related_test.go` | 70 | Tests cluster-name and task-definition related checkers from Fields map and RawStruct. |  | **keep**. |
| `tests/unit/aws_eks_related_test.go` | 70 | Tests nodegroup and alarm dimension checkers for EKS clusters. |  | **keep**. |
| `tests/unit/aws_eni_related_test.go` | 70 | Tests five navigable-field registrations (VpcId, SubnetId, Groups.GroupId, Attachment.InstanceId, Association.AllocationId) and instance checker. |  | **keep**. |
| `tests/unit/aws_iam_group_members_test.go` | 70 | Column width/title exact-value assertions, PasswordLastUsed override to "N/A (not in API)" constant, date format, nil fields, IAM Marker pagination, RawStruct — field-mapping bugs would be caught. | Gap: no test verifying pagination Marker is actually forwarded in GetGroup input | **expand**: capture the Marker passed into page-2 GetGroup input and assert it equals page-1's NextMarker. |
| `tests/unit/aws_identity_test.go` | 70 | Tests the two-call CallerIdentity fetch (STS + IAM alias) including ARN parsing for assumed-role vs IAMUser formats, and alias-not-found fallback. |  | **keep**. |
| `tests/unit/aws_kms_related_test.go` | 70 | Tests KMS→EBS/RDS/Secrets ARN-based matching across three resource types using RawStruct. Multi-type coverage for one checker pattern. |  | **keep**. |
| `tests/unit/aws_natgateways_test.go` | 70 | Tests public/private connectivity, missing-EIP path, create-time formatting, and pending state. | Missing error case. | **expand**: add API error path + nil CreateTime. |
| `tests/unit/aws_ng_related_test.go` | 70 | Tests navigable fields (ClusterName→eks, NodeRole→role) + related checkers for EKS, ASG, and IAM role via RawStruct matching. | Nothing obvious. | **keep**. |
| `tests/unit/aws_rds_snap_related_test.go` | 70 | Tests navigable field (DBInstanceIdentifier→dbi) + field-path resolution + CW alarm dimension match. | Nothing obvious. | **keep**. |
| `tests/unit/aws_rds_test.go` | 70 | Tests two engine types (mysql/postgres), MultiAZ flag, endpoint parsing, and missing-endpoint guard. | Missing error-return path. | **expand**: add API error path + aurora engine + nil InstanceStatus. |
| `tests/unit/aws_s3_test.go` | 70 | Tests bucket name, creation date, and three-bucket parse. | Field coverage is narrow; does not verify `region` or nil-date handling. | **expand**: add nil CreationDate, region field (if extracted), error path. |
| `tests/unit/aws_secrets_related_test.go` | 70 | Tests KMS key ID match, Lambda rotation ARN match, navigable-field absence. | Missing no-match paths for KMS and Lambda. | **expand**: add KMS no-match and Lambda no-match cases. |
| `tests/unit/aws_sns_related_test.go` | 70 | Tests CW alarm TopicArn dimension match, SQS queue ARN match, trail SNS match, no-match paths. | Nothing notable. | **keep**. |
| `tests/unit/aws_sqs_related_test.go` | 70 | Tests CW alarm QueueName dimension match via Pattern C cache lookup. | Nothing notable. | **keep**. |
| `tests/unit/aws_subnet_related_test.go` | 70 | Tests EC2, ELB, and NAT related checkers; covers match/no-match using cache for all three patterns. | Missing RDS checker coverage. | **expand**: add subnet→rds and subnet→ecs-task coverage. |
| `tests/unit/aws_subnets_test.go` | 70 | Tests available/pending states, public IP flag, ARN, owner, and empty-tags path. | Missing error-return path. | **expand**: add API error path. |
| `tests/unit/aws_tgw_related_test.go` | 70 | Tests RTB related checker for transit gateway via route TransitGatewayId matching. | Single checker tested; if more TGW related types exist they are uncovered. | **audit**: enumerate all registered TGW related types and expand coverage to each. |
| `tests/unit/cache_registry_validation_test.go` | 70 | Documents and enforces that (a) unknown cache keys don't silently survive and (b) all registered resource types survive round-trip; the live-registry test auto-covers new resource types. | `TestCache_RejectsUnknownResourceKeys` documents a known gap rather than enforcing a fix. | **land the fix**: dispatch coder to reject unknown cache keys in the loader, then flip the test from documentation to enforcement. |
| `tests/unit/ctdetail_summarize_ec2_test.go` | 70 | RunInstances all-fields-emitted with navigable/non-navigable distinction; the TargetType assertion for imageId→ami catches a real mapping bug. | Only partial read; RunInstances alone doesn't cover TerminateInstances, StartInstances, etc. | **expand**: add TerminateInstances, StartInstances, StopInstances, ModifyInstanceAttribute, AttachVolume events. |
| `tests/unit/detail_resize_test.go` | 70 | pendingRelatedDispatch flag set/clear on narrow→wide resize and regression guard against double-dispatch on first paint; catches a subtle async dispatch ordering bug. | Narrow scenario coverage only. | **expand**: add wide→narrow→wide cycle, rapid resize sequence, and simultaneous resize+related-msg ordering. |
| `tests/unit/ec2_related_view_golden_test.go` | 70 | Golden-snapshot test for the EC2 related-view render; catches unintended visual regressions in the two-column layout. | Gap: only EC2 covered; other resource types have no golden snapshots. | **expand**: add golden snapshots for 4 additional resource-type classes (DB: RDS, Network: VPC, Compute: Lambda, IAM: Role) to cover the distinct two-column layouts. Store under `tests/unit/testdata/golden/`. |
| `tests/unit/filter_test.go` | 70 | Tests FilterResources with regex, empty, special chars, case-insensitive matching across field values — solid correctness coverage. | Gap: only `views.FilterResources` tested; no regex-error path or very-long-name edge case. | **expand**: add invalid-regex graceful-degrade case + 10KB name fuzz. |
| `tests/unit/issue140_story_render_contract_test.go` | 70 | Render-contract style tests for EC2 QA stories from issue #140; catches visual regressions in detail rendering. | Nothing obvious. | **keep**. |
| `tests/unit/left_column_preview_regressions_test.go` | 70 | Preview-design regression tests for the left column in EC2 detail at multiple widths; catches rendering regressions. | Nothing obvious. | **keep**. |
| `tests/unit/qa67_cross_cutting_test.go` | 70 | Broad fuzz-sampler for inputs in multiple states, empty-list safety for all resource types × ops, and filter-clear round-trip are all genuinely regression-prone. | gap: K.2/K.3/K.7 have soft checks (Logf not Errorf); catch rate lower than it appears | **tighten K.2, K.3, K.7**: convert `t.Logf` post-conditions to `t.Errorf`. If genuinely non-deterministic, delete the soft assertion. |
| `tests/unit/qa_cf_test.go` | 70 | CloudFront field mapping with exact assertions (aliases, domain, HTTP version); tests both Deployed and InProgress states. | gap: partial read; standard pattern, moderate value | **keep**. |
| `tests/unit/qa_codebuild_test.go` | 70 | Two-step fetch (ListProjects → BatchGetProjects) is architecturally non-trivial; source type mapping (CodeCommit vs GitHub) tested explicitly. | gap: partial read — scored on first 60 lines | **keep**. |
| `tests/unit/qa_detail_v220_test.go` | 70 | Covers v2.2.0 resource types (CF, Athena, CloudTrail, CodeBuild, etc.) with realistic SDK struct builders — broad regression coverage for new resource types. | gap: partial read — scored on imports/structure only | **keep**; coverage is absorbed into `qa_detail_paths_test.go` over time — consider merging if that file gains breadth. |
| `tests/unit/qa_docdb_snapshots_test.go` | 70 | DocDB snapshot fetcher with exact field assertions (snapshot type, storage type, status) and nil-field guard — standard pattern but catches real mapping bugs. | nothing | **keep**. |
| `tests/unit/qa_docdb_test.go` | 70 | DocDB cluster list with multi-status color tests (available/creating/failed), uses RawStruct-populated fixtures — catches status coloring regressions. | gap: partial read — scored on first 60 lines | **keep**. |
| `tests/unit/qa_mainmenu_nav_test.go` | 70 | Filter mode entry/clear, filter text in header, command mode — menu navigation state machine correctness. | gap: partial read | **keep**. |
| `tests/unit/qa_pagination_hint_test.go` | 70 | Directly tests the filter-aware hint variant (T012) as a TDD failing test alongside currently-passing variants; the filter-active hint is uniquely regression-prone. | Only 3 hint variants tested; loading state after second page not covered. | **expand**: add the mid-pagination-loading hint variant (between page-1 return and page-2 dispatch). |
| `tests/unit/qa_pagination_services_test.go` | 70 | Tests 15 service-layer paginated fetchers (rds, redis, docdb, efs, r53, cf, etc.) covering Marker vs NextToken patterns across diverse SDK clients. | No assertion that fetcher stops calling API once NextToken is empty. | **expand**: add a call-count assertion — page-2 returns empty token → verify no page-3 call was made. |
| `tests/unit/qa_r53_test.go` | 70 | Tests R53 hosted zone fetch and the drill-down child view navigation flow; covers the zone_id stripping ("/hostedzone/" prefix handling). | No test for private vs public zone rendering distinction. | **expand**: add private-zone fixture + assertion that `private=true` badge or field is visible. |
| `tests/unit/qa_s3_test.go` | 70 | Tests S3 bucket list view and child view drill-down navigation; catches S3 ChildViewDef ContextKeys regression and folder drill condition bugs. | Good breadth of navigation paths. | **keep**. |
| `tests/unit/qa_search_views_test.go` | 70 | Tests search embedded in DetailModel and YAMLModel across multiple resource types; catches search integration regressions when either view is modified. | Good multi-resource-type coverage for search integration. | **keep**. |
| `tests/unit/related_escape_regression_test.go` | 70 | Regression guard that Esc from related-filtered list returns to source detail without clearing the filter; catches UX regression. | Nothing obvious. | **keep**. |
| `tests/unit/tui_root_test.go` | 70 | Tests root model construction, WindowSizeMsg, NavigateMsg routing; core wiring regression coverage. | Nothing obvious. | **keep**. |
| `tests/unit/cache_test.go` | 71 | Comprehensive CRUD round-trip coverage for the cache package including TTL, sanitization, overwrite, and all-resource-types path; catches field-mapping bugs on serialization. | `TestCache_DefaultTTL_IsOneHour` is a trivial constant check (low value). | **keep; delete the TTL constant check**. |
| `tests/unit/ctdetail_summarize_s3_test.go` | 71 | Per-event residual field assertions for S3 (PutObject/GetObject/PutBucketPolicy); catches cases where cleaned params lose fields or emit wrong keys. | Partial read only; but sample tests are exact-value assertions, not isEmpty checks. | **expand**: add PutBucketAcl, DeleteBucket, ListBuckets, GetBucketVersioning events (high-volume S3 audit events). |
| `tests/unit/detail_related_test.go` | 71 | ToggleRelated cmd dispatch semantics, RelatedCheckResultMsg state update, right-column visibility transitions; complementary to focus tests with cmd-level assertions. | Nothing significant. | **keep**. |
| `tests/unit/qa_backup_test.go` | 71 | Exact field assertions (plan_name, plan_id, last_execution nil/non-nil distinction), ResourceTypeDef column registration checks including exact widths — catches both mapping and registration bugs. | gap: ResourceTypeDef column-width assertions are brittle to intentional changes | **shrink**: delete the exact-width assertions; keep column-count, column-key, and column-title assertions. Width is a layout concern, not a mapping concern. |
| `tests/unit/qa_fetch_test.go` | 71 | Completeness test: every AWS service client can be instantiated with real SDK without panicking — regression guard against import/init order bugs. | gap: partial read — scored on imports only; actual test logic not visible | **keep** — import-order regression guard is load-bearing for release CI. |
| `tests/unit/qa_pagination_infra_test.go` | 71 | Paginated fetch tests for 15 infra resource types (ecs, asg, vpc, nat, etc.) with multi-page scenarios; broad coverage of distinct pagination token patterns. | No assertions that the fetcher passes the received token back on subsequent calls. | **expand**: add input-token assertions for each fetcher (same as `qa_pagination_monitoring_test.go`). |
| `tests/unit/app_cancellation_test.go` | 72 | Tests that `tui.New` returns a live context, `QuitMsg` cancels it, no `context.Background()` in production source files, and pre-cancelled context prevents IAM calls — a real correctness property that no other test covers. | `context.Background()` scan is a string-grep heuristic, not a behavioral test. | **keep + tighten**: replace the grep heuristic with a real behavioral test — mock an AWS client and verify fetchers receive the cancelled ctx and abort. |
| `tests/unit/aws_acm_related_test.go` | 72 | Tests real cache-matching logic for `acm→cf` (ARN comparison) including empty-ARN, cache-miss, and three stub-zero checkers. Catches misrouted ARN matching bugs. |  | **audit stubs**: verify the three stub-zero checkers are genuinely undeterminable. If any is viable, upgrade. Otherwise keep. |
| `tests/unit/aws_alarm_history_test.go` | 72 | Solid field-mapping, nil-guard, newline-stripping, pagination contract, and continuation-token forwarding tests for a real fetcher. Registration tests are minor completeness guards but don't dominate. | Gap: no test for AlarmType enum variants mapping to Status | **expand**: add MetricAlarm vs CompositeAlarm enum mapping assertion. |
| `tests/unit/aws_ami_test.go` | 72 | Covers `FetchAMIs` field extraction with exact values, plus `FetchAMIByID` API contract (no Owners filter). The `capturingDescribeImagesClient` catches a regression if owners filtering creeps in. |  | **keep**. |
| `tests/unit/aws_asg_activities_test.go` | 72 | Same high-quality pattern as alarm_history — covers field mapping, nil fields, pagination, token capture. Status enum variations (Failed/Cancelled/InProgress) covered in pagination tests. | Gap: no explicit multi-status assertion on Field[status_code] for all ASG status enum values | **expand**: add table-driven test covering all ActivityStatusCode enum values (PendingSpotBidPlacement, WaitingForSpotInstanceRequestId, WaitingForSpotInstanceId, WaitingForInstanceId, PreInService, InProgress, WaitingForELBConnectionDraining, MidLifecycleAction, WaitingForInstanceWarmup, Successful, Failed, Cancelled, WaitingForConnectionDraining). |
| `tests/unit/aws_dbc_related_test.go` | 72 | Tests dimension-based alarm match, naming-convention log match (multi-log-group case), and stub secrets checker. Two-log-group assertion catches count-off-by-one. |  | **audit stub**: DocDB cluster → Secrets Manager requires DescribeDBCluster which doesn't expose SecretArns. Likely legit stub. Keep. |
| `tests/unit/aws_ecs_clusters_related_test.go` | 72 | Tests navigable-field registration and dimension-based alarm match, CFN stack reference. Solid multi-pattern coverage. |  | **keep**. |
| `tests/unit/aws_elb_related_test.go` | 72 | Tests navigable-field registration (VpcId, SecurityGroups, SubnetId) and ACM/WAF/alarm dimension checkers. Breadth is solid. |  | **keep**. |
| `tests/unit/aws_error_wrapping_test.go` | 72 | Tests that every fetcher wraps errors with `errors.Is`-compatible wrapping and a descriptive prefix. Catches any fetcher that returns bare errors. |  | **keep** — cross-cutting contract guard. |
| `tests/unit/aws_lambda_invocation_logs_test.go` | 72 | Status classification (6 keywords), StartTime bound assertion, filter pattern with requestID, nil fields, newline stripping, RawStruct, timestamp formatting — strong for a level-2 child. | Gap: no pagination/NextToken forwarding test for FilterLogEvents | **expand**: add NextToken forwarding assertion — page-1 returns token, page-2 receives it as input. |
| `tests/unit/aws_lambda_related_test.go` | 72 | Tests navigable Role field, log-group naming convention, and alarm dimension matcher. Multi-pattern coverage. |  | **keep**. |
| `tests/unit/aws_nat_related_test.go` | 72 | Tests navigable field registration (VpcId, SubnetId, AllocationId) + field-path resolution via demo fixtures. | Only navigable-field coverage; no related checker match tests. | **expand**: add real checker match tests — nat→vpc, nat→subnet, nat→eip via cache. |
| `tests/unit/aws_rds_events_test.go` | 72 | Tests three event types with categories, timestamps, ARNs, and nil-date guard. | Nothing notable. | **keep**. |
| `tests/unit/aws_role_policies_test.go` | 72 | Tests two-page paginated ListAttachedRolePolicies, exact policy ARN/name fields, and error propagation. | Nothing notable. | **keep**. |
| `tests/unit/aws_rtb_related_test.go` | 72 | Tests navigable fields (VpcId, Associations.SubnetId, Routes.NatGatewayId) with path resolution via demo fixtures. | Nothing obvious. | **keep**. |
| `tests/unit/aws_s3_related_test.go` | 72 | Tests trail→s3 (match via S3BucketName), CloudFront→s3 (match via OriginDomainName), and no-match paths. | Nothing notable. | **keep**. |
| `tests/unit/aws_sg_related_test.go` | 72 | Tests VpcId navigable field + EC2, ELB, RDS related checkers via cache/RawStruct lookup. | Nothing notable. | **keep**. |
| `tests/unit/aws_sg_test.go` | 72 | Tests inbound/outbound rule counts, VPC association, owner, and empty-rules path. | Missing error-return case. | **expand**: add API error + nil VpcId guards. |
| `tests/unit/aws_sns_topic_subscriptions_test.go` | 72 | Tests three subscription protocols (email, https, sqs) with confirmed/pending status and pagination. | Nothing notable. | **keep**. |
| `tests/unit/aws_sqs_test.go` | 72 | Tests list+describe two-step fetch with exact queue attribute values (message count, delay, ARN). | Missing single-queue and error-on-GetQueueAttributes paths. | **expand**: add single-queue path and error-on-GetQueueAttributes (per-queue failure should not abort the whole fetch). |
| `tests/unit/aws_targetgroups_test.go` | 72 | Tests both HTTP and HTTPS target groups with instance/IP target types, health-check interval fields, and ARN mapping. | Missing nil-port guard. | **expand**: add nil Port + nil HealthCheckIntervalSeconds guards. |
| `tests/unit/aws_vpc_related_test.go` | 72 | Tests subnet, SG, RTB, NAT, VPCE, ELB, RDS, and TGW related checkers for VPC using Pattern C cache lookups. | Broad coverage; gap is the nil-RawStruct path. | **expand**: add nil-RawStruct source resource path (should return Count=0 or -1 per contract, not panic). |
| `tests/unit/aws_vpc_test.go` | 72 | Tests field-mapping for both states (available/pending) including tags, default VPC flag, tenancy, and nil-client error guard. | Missing `error` path (mock returns err). | **expand**: add mock-returns-error path. |
| `tests/unit/aws_vpce_related_test.go` | 72 | Tests interface vs gateway endpoint type with subnet, SG, ENI, and RTB related checkers — type-conditional logic is regression prone. | Could add test for endpoint with empty SubnetIds but non-empty RouteTableIds. | **expand**: add the asymmetric subnet/rtb case and the GatewayLoadBalancer endpoint type. |
| `tests/unit/bug_cfn_key_collision_test.go` | 72 | Catches a real key-binding collision bug (CFN child view "r" shadowing ToggleRelated); assertion is exact and would catch a silent regression if the key were changed back. | Nothing significant. | **keep + generalize**: extend to a cross-cutting test that asserts NO child-view key collides with a top-level key binding (ToggleRelated, Refresh, etc.) for ANY resource type. |
| `tests/unit/config_test.go` | 72 | Comprehensive config loading tests covering lookup chain priority, partial override fallback, defaults, views dir round-trip, ConfigDir/EnsureConfigDir/ConfigFilePath utilities, and env-var non-fallthrough. | TestViewsDir_RoundTrip_MatchesDefaults reads from .a9s/views/ which requires viewsgen to have run — can be fragile in CI. | **stabilize**: either (a) have the test invoke viewsgen programmatically before asserting, or (b) t.Skip with a clear message if the dir is missing. No silent false-pass. |
| `tests/unit/demo_app_test.go` | 72 | App-level integration: Init→ClientsReadyMsg, NavigateMsg→ResourcesLoadedMsg, blocked commands (ctx/region), reveal pathway for Secrets and SSM, refresh idempotency — catches app-wiring regressions not covered by unit tests. | Gap: no test for child-view navigation (EnterChildViewMsg path) in demo mode | **expand**: add EnterChildViewMsg path test for at least 3 child types (s3→s3_objects, ecs_cluster→ecs_services, r53→r53_records) to cover context-key resolution patterns. |
| `tests/unit/detail_boundary_spec008_test.go` | 72 | Spec-008 j/k cursor boundary clamping; guarded by build tag until FieldCursor() getter is added — real spec compliance test once the build tag is removed. | Build tag means the test is dormant until manual intervention. | **unblock**: dispatch coder to land `FieldCursor()` getter; then remove the build tag. |
| `tests/unit/ec2_detail_design_contract_test.go` | 72 | Validates detail rendering against the approved design contract using a full demo model + explicit field assertions; catches layout regressions in the two-column view. | Nothing obvious — contract test is tight. | **keep**. |
| `tests/unit/fieldpath_unexported_test.go` | 72 | Tests ExtractSubtree across all AWS SDK types including unexported-field handling; panic-prevention coverage on real SDK structs. | Nothing obvious — good breadth. | **keep**. |
| `tests/unit/issue119_140_regressions_test.go` | 72 | Stacked-width (80-99 col) related panel toggle regression guard; catches layout bugs at non-standard widths. | Nothing obvious. | **keep**. |
| `tests/unit/issue140_scenarios_golden_test.go` | 72 | Golden scenarios for issue #140 (related navigation) rendering contracts. | Nothing obvious. | **keep**. |
| `tests/unit/preview_design_regression_test.go` | 72 | Tests design-preview render output across all resource types at multiple terminal sizes; catches visual regressions in design mockups. | Nothing obvious. | **keep**. |
| `tests/unit/qa67_terminal_size_test.go` | 72 | Tests real boundary conditions (exact 60-col, 7-row min, resize transitions) that could expose off-by-one bugs in the narrow/short guards; crash tests on resize across all views add real regression value. | gap: boundary checks are mostly crash/non-crash rather than content-correct assertions | **tighten**: replace crash-only checks with content assertions — at 60 cols frame title must be truncated with "…", at 7 rows all content must fit, etc. |
| `tests/unit/qa_apigw_test.go` | 72 | Field-mapping tests with exact value assertions catch real mapping bugs (protocol string, endpoint URL, RawStruct type assertion); tests both HTTP and WEBSOCKET protocol paths. | gap: no nil-pointer guard test for nil ApiId/Name | **expand**: add nil ApiId, nil Name, nil ProtocolType guards. |
| `tests/unit/qa_configurable_views_test.go` | 72 | Tests config-driven detail rendering with realistic SDK structs for multiple resource types — catches wrong field paths in views.yaml. | gap: partial read — scored on first 60 lines | **keep**. |
| `tests/unit/qa_ec2_test.go` | 72 | EC2 list column layout, state/type rendering, wide terminal; documents the RawStruct fallback behavior explicitly — valuable as regression anchor for config-driven column changes. | gap: several columns expected empty due to RawStruct=nil; tests document known limitations | **refactor**: populate RawStruct in fixtures so columns render fully; this converts "documents limitations" into "catches regressions". |
| `tests/unit/qa_ecs_tasks_test.go` | 72 | Multi-step ECS tasks fetch (ListClusters → ListTasks → DescribeTasks) — non-trivial multi-API aggregation that is regression-prone; exact field assertions. | gap: partial read | **keep**. |
| `tests/unit/qa_field_alias_test.go` | 72 | Field alias registration/apply with exact assertions: alias added, original preserved, no-overwrite guarantee — catches alias logic bugs directly. | gap: small file, limited scope | **keep**. |
| `tests/unit/qa_pagination_compute_test.go` | 72 | Tests paginated fetchers for ec2/lambda/s3 with real SDK types and both single-page and multi-page flows; catches NextToken/Marker propagation bugs. | No test verifying token is forwarded on the second call (input assertion). | **expand**: add input-token assertions (same pattern as `qa_pagination_monitoring_test.go`). |
| `tests/unit/qa_r53_records_test.go` | 72 | Tests the full child view drill-down flow including YAML view and field extraction via fieldpath; catches ContextKeys mapping regressions. | Fixtures are minimal (few record types). | **expand**: add fixtures for AAAA, TXT, SRV, NS, SOA, CAA, DS records to cover the full R53 type taxonomy. |
| `tests/unit/qa_search_component_test.go` | 72 | TDD tests for SearchModel API (SetContent, SetQuery, Apply, Next/Prev); drives out the contract before implementation, catches match-count and highlight bugs. | Good TDD driver; value depends on SearchModel not yet existing. | **keep if SearchModel not yet landed; else rescore**. |
| `tests/unit/qa_status_colors_test.go` | 72 | Tests ANSI color output for all status values (running/stopped/terminated/pending/available) with exact color comparison; catches palette mapping regressions. | Solid regression guard for Tokyo Night status palette. | **keep**. |
| `tests/unit/related_fetch_test.go` | 72 | Tests FetchRelatedTarget cache-hit, cold-fetch, error paths; catches regressions in the generic fetch helper. | Nothing obvious. | **keep**. |
| `tests/unit/resourcelist_ami_stub_test.go` | 72 | Tests StubCreator-based auto-navigate-to-detail on empty result; catches hardcoded ShortName=="ami" regression. | Nothing obvious. | **keep**. |
| `tests/unit/rightcolumn_root_filter_regression_test.go` | 72 | Tests that root-level related filter is not applied when navigating from a related-filtered list; catches filter-leak regression. | Nothing obvious. | **keep**. |
| `tests/unit/s3_object_pagination_test.go` | 72 | Tests FetchS3Objects single-page pagination contract (one API call per invocation); catches over-fetching regressions. | Nothing obvious. | **keep**. |
| `tests/unit/styles_is_dim_row_color_test.go` | 72 | Tests IsDimRowColor across all status values including ct-events severity ladder and EC2/RDS lifecycle; catches new status value omissions. | Nothing obvious — good coverage of the status→color mapping. | **keep**. |
| `tests/unit/tui_resourcelist_test.go` | 72 | Tests ResourceListModel with sort, filter, cursor, status color, dim rows; comprehensive list-view coverage. | Nothing obvious. | **keep**. |
| `tests/unit/views_resourcelist_ctrlz_hint_test.go` | 72 | Tests that ctrl+z appears in BottomHints() for all resource types; catches omission of discoverability hint. | Nothing obvious. | **keep**. |
| `tests/unit/aws_asg_related_test.go` | 73 | Tests CloudWatch dimension match and EKS nodegroup ASG-name match via RawStruct, both with empty-ID and nil-cache guards. Catches realistic mapping bugs across two different patterns. |  | **keep**. |
| `tests/unit/aws_cfn_events_test.go` | 73 | Thorough: field mapping, nil optional fields, newline stripping, timestamp format, RawStruct, pagination (two-page), max-cap, column defs, registration, parent child-def key assertion. | Gap: no test verifying the stack_name is passed as StackName in the API input | **expand**: capture the StackName input on mock calls and assert it matches the parent resource name. |
| `tests/unit/aws_cfn_resources_test.go` | 73 | Covers drift-status enum mapping, nil DriftInformation, nil PhysicalResourceId, timestamp, RawStruct, multi-page pagination with callIdx verification, and column key ordering. | Gap: no test that DescribeStackResources uses the continuation token as NextToken in the API request | **expand**: capture and assert NextToken is forwarded as input on page-2. |
| `tests/unit/aws_dbi_related_test.go` | 73 | Tests alarm dimension match, RDS snapshot DBInstanceIdentifier match via RawStruct, and stub secrets/kms/subnet checkers. Real pattern-C and pattern-D paths tested. |  | **audit stubs**: DBI→Secrets is undeterminable from DescribeDBInstances output; DBI→KMS and DBI→Subnet ARE extractable from RawStruct (KmsKeyId, DBSubnetGroup.Subnets). Upgrade the KMS and Subnet checkers to read RawStruct; keep Secrets as-is. |
| `tests/unit/aws_ecr_images_test.go` | 73 | Based on visible portion: digest-short computation, scan-finding severity counts, size formatting, nil scan status, pagination, continuation-token capture, column/registration tests — solid field-mapping coverage. | Gap: test that images with no tags show a specific fallback value | **expand**: add no-tags image → "<untagged>" or equivalent assertion. |
| `tests/unit/aws_r53_records_test.go` | 73 | Tests A, CNAME, MX records with multi-value, TTL, alias targets, and pagination. | Nothing notable. | **expand**: add AAAA, TXT, SRV, NS, SOA, CAA, DS (completes R53 record type taxonomy). |
| `tests/unit/aws_sfn_execution_history_test.go` | 73 | Tests three event types (ExecutionStarted, TaskScheduled, TaskSucceeded) with timestamp formatting, event type mapping, and pagination. | Nothing notable. | **expand**: add TaskFailed, ExecutionFailed, ExecutionTimedOut, ExecutionAborted event types. |
| `tests/unit/aws_tg_health_test.go` | 73 | Tests four health states (healthy, unhealthy, draining, unused) with port and AZ fields. | Nothing notable. | **expand**: add `initial` state (fifth enum value) + nil AZ guard. |
| `tests/unit/aws_toplevel_pagination_batch2_test.go` | 73 | Second pagination batch: SG, ELB, Redis, RedShift, DocDB, RDS, plus error-on-second-page cases. | Solid; gap is ELB second-page error propagation. | **expand**: add ELB error-on-page-2 assertion verifying partial results are returned with error. |
| `tests/unit/aws_toplevel_pagination_batch4_test.go` | 73 | Fourth batch: CFN, ASG, CloudFront, CWAlarms, ECR, EventBridge, R53, Secrets, SFN, SSM pagination. | Similar quality to batch3; no coverage gaps stand out. | **keep**. |
| `tests/unit/aws_trail_related_test.go` | 73 | Tests S3 bucket, SNS, KMS, and CWLogs related checkers for trail using RawStruct field extraction; covers match/no-match/nil patterns. | Nothing obvious. | **keep**. |
| `tests/unit/cache_snapshot_test.go` | 73 | Covers IsTruncated propagation from cache entry to related checker; catches a subtle semantic bug where 0 local matches + truncated cache should return Count=-1, not 0. | Only two scenarios; could test more checker types. | **expand**: extend to all related checker families (Pattern C cache, Pattern F RawStruct, Pattern D alarm dimension) to ensure the truncation contract is uniform. |
| `tests/unit/ctdetail_summarize_generic_test.go` | 73 | Purity (no mutation), nil/empty input contracts, no-navigable-rows guarantee, panic safety on heterogeneous types, and eventName-ignored invariant — all are real behavioral contracts. | Nothing significant. | **keep**. |
| `tests/unit/qa_demo_completeness_test.go` | 73 | For every resource type: verifies each detail-view path in views.yaml resolves to a non-empty value via fieldpath.ExtractSubtree on fixture RawStruct — catches missing/wrong SDK field paths in fixtures. | gap: skips types with no demo fixtures rather than failing | **tighten**: convert skip to hard-fail (every registered type MUST have demo fixtures). Silent skips hide real gaps. |
| `tests/unit/qa_retry_probe_test.go` | 73 | Tests RetryOnThrottle contract with ThrottlingException, non-retryable errors, and max-attempts exhaustion; catches retry logic divergence if coder changes config. | Tests pass before the production wiring — value is as a contract guard post-implementation. | **keep**. |
| `tests/unit/aws_cb_build_logs_test.go` | 74 | Strong status-classification table covers 10 message patterns; nil-field, message-stripping, timestamp, RawStruct, and registration tests all add value. | Gap: no test for pagination/NextToken forwarding to GetLogEvents | **expand**: add NextToken input-forwarding assertion. |
| `tests/unit/aws_cb_related_test.go` | 74 | Tests ARN-last-segment role matching, dual-path log matching (explicit GroupName + naming convention), nil-ServiceRole guard, and stub pipeline checker. Catches real ARN parsing bugs. |  | **upgrade stub**: CB→pipeline IS determinable by scanning Pipeline stage actions for matching ProjectName. Dispatch coder to implement. |
| `tests/unit/aws_eb_rule_targets_test.go` | 74 | ArnToResourceName and ComputeInputSummary helper tables, nil-field, RawStruct, empty/error paths, and registration tests — the helper-function coverage is genuinely valuable. | Gap: no test for InputPath priority vs InputTransformer when both are set | **expand**: add test where both InputPath and InputTransformer are set; assert InputTransformer takes priority (per EventBridge docs). |
| `tests/unit/aws_pipeline_stages_test.go` | 74 | Tests stage+action flattening, status mapping for all pipeline action states, and nil-LatestExecution guard. | Nothing notable. | **keep**. |
| `tests/unit/aws_redis_test.go` | 74 | Tests engine-type filter (redis vs memcached), missing endpoint guard, and multi-node vs single-node paths. | Missing error-path test. | **expand**: add API error path. |
| `tests/unit/aws_sfn_executions_test.go` | 74 | Tests three-field child fetch (name, status, timestamps, redrive count, item count), empty-list guard, nil-stop-date guard, and pagination. | Nothing notable. | **keep**. |
| `tests/unit/aws_tg_related_test.go` | 74 | Tests ELB, ECS service, ASG, and alarm related checkers for target groups; ARN matching logic is regression-prone. | Demo-mode smoke tests could be tighter on count values. | **tighten demo-mode smoke**: replace `len(results) > 0` with explicit expected target types (ELB, ECS, ASG, alarm). |
| `tests/unit/aws_toplevel_pagination_batch3_test.go` | 74 | Third batch: APIGWv2, Athena, Backup, CodeBuild, CodeArtifact, CodePipeline, EFS, EKS, EB, Glue, IAM, MSK, SES, SNS, WAF pagination. | Broad coverage; pagination reset-to-nil NextToken edge case not tested. | **expand**: add a "token-returned-then-nil" edge case for one representative fetcher per token-style. |
| `tests/unit/bug_reveal_related_column_resize_test.go` | 74 | Two complementary resize tests: auto-show after narrow→wide, and explicit-hide-persists-across-resize; both catch realistic user-choice and layout regressions. | Nothing significant. | **keep**. |
| `tests/unit/child_view_resourcelist_test.go` | 74 | Covers the full context-resolution matrix (ID, Name, @parent.x, Fields), DrillCondition true/false, non-enter key routing, and FrameTitle with count — catches real mapping bugs. | Nothing significant. | **keep**. |
| `tests/unit/detail_render_unit_test.go` | 74 | Tests RawYAML, PlainContent, renderFromConfig, and the sorted-Fields fallback path — these were 0% coverage before; catches rendering regressions in the detail content pipeline. | Nothing significant. | **keep**. |
| `tests/unit/detail_selected_row_visibility_test.go` | 74 | Visual regression: selected row must have background highlight and must NOT apply key-tint color (low-contrast risk); tests ANSI escape sequences directly — catches color regression class. | Brittle to exact ANSI escape sequences; could break on style refactors. | **refactor**: use a semantic helper (e.g. `hasBackgroundHighlight(line)`, `appliesKeyTint(line)`) instead of matching raw ANSI bytes. Preserves catch rate without brittleness. |
| `tests/unit/ec2_status_checks_test.go` | 74 | Tests EC2 instance status check merge (system/instance status fields), list prefix glyphs, detail section rendering — real fetcher integration. | Nothing obvious. | **keep**. |
| `tests/unit/issue119_scenarios_golden_test.go` | 74 | Golden scenarios for issue #119 (stacked layout) with golden-file snapshots; solid regression coverage. | Nothing obvious. | **keep**. |
| `tests/unit/qa67_malformed_data_test.go` | 74 | Unicode names, zero timestamps, 256-char tags, empty IDs, all-nil RawStruct exercised across all resource types — these are the exact inputs production data occasionally produces. | gap: unicode test only checks non-empty output, not layout correctness | **expand**: add width-correctness assertions (lipgloss.Width of unicode name == expected cell count), and no-frame-corruption for 256-char tags (assert frame edges still align). |
| `tests/unit/qa_26_search_integration_test.go` | 74 | Exit search (G), scroll-to-match (K), word-wrap interaction (L), component reuse (N), other keys during search (O), terminal resize (Q) — integration coverage for a new feature. | gap: partial read | **keep**. |
| `tests/unit/qa_cache_user_stories_test.go` | 74 | Warm re-entry from cache restores sort+cursor; load-more updates warm cache; hard assertions on exact resource ID and view label — high value for cache correctness. | gap: partial read — scored on first 80 lines | **keep**. |
| `tests/unit/qa_detail_services_test.go` | 74 | Realistic SDK struct builders for Lambda, ACM, DDB, SES, ECS service, IAM role, SSM, ALB, SNS, ASG — used as fixtures in detail path tests. | gap: these are fixture builders, not test functions themselves; indirect value | **keep (infra)** — fixture builders for `qa_detail_paths_test.go`. Exclude from scoring. |
| `tests/unit/qa_detail_test.go` | 74 | S3 bucket detail view renders CreationDate with correct timestamp format, FrameTitle matches resource name — field-level correctness tests with exact string assertions. | gap: partial read — covers S3 only in sample; full file likely covers more types | **keep**. |
| `tests/unit/qa_filtering_test.go` | 74 | Filter across multiple resource types with exact match count assertions, regex special chars, case sensitivity, ESC clear — catches filter logic bugs end-to-end. | gap: partial read | **keep**. |
| `tests/unit/qa_kms_test.go` | 74 | KMS multi-step fetch (ListKeys → DescribeKey) with CUSTOMER filter logic — the filtering step is non-trivial and regression-prone; catches bug where AWS-managed keys are included. | gap: partial read | **keep**. |
| `tests/unit/qa_sort_order_test.go` | 74 | Tests ascending/descending sort on multiple columns with exact row-order verification in rendered output; catches sort comparator bugs. | Strong: exact position assertions, not just "sorted" boolean. | **keep**. |
| `tests/unit/registry_init_order_test.go` | 74 | Tests that all ChildViewDef.ChildType names are registered; catches silent nil-type panics at runtime for new child types. | Nothing obvious — solid completeness guard. | **keep**. |
| `tests/unit/s3_pagination_test.go` | 74 | Tests FetchS3Buckets pagination (ContinuationToken) and single-page case; catches pagination regressions in S3 fetcher. | Nothing obvious. | **keep**. |
| `tests/unit/aws_alarm_related_test.go` | 75 | Tests SNS deduplication across three action lists plus non-SNS ARN filtering, and ASG dimension-based match. Deduplication and ARN-prefix filter are real bug vectors. |  | **keep**. |
| `tests/unit/aws_cb_builds_test.go` | 75 | Two-client mock pattern (list + batch), duration calc, in-progress duration, SHA truncation, log fields, nil fields, pagination, and continuation-token capture — well above average coverage. | Gap: no test for multiple builds in BatchGetBuilds response ordering | **expand**: add 3-build BatchGetBuilds response and assert returned order matches the input ID order (or documented sort). |
| `tests/unit/aws_cf_related_test.go` | 75 | Tests five checkers including S3 origin domain parsing, ELB dns_name match, WAF WebACLId ARN match, ACM ViewerCertificate ARN match, each with nil-cache guard. Many realistic field-path bugs catchable here. |  | **keep**. |
| `tests/unit/aws_ec2_related_test.go` | 75 | Tests many checkers including the `EC2RelatedCheckers_NoUnknownCounts` integration assertion plus individual navigable-field and dimension-based checkers. Breadth makes it high-value. |  | **keep**. |
| `tests/unit/aws_nodegroups_test.go` | 75 | Covers three-step fan-out fetch (ListClusters→ListNodegroups→DescribeNodegroup) with 3 node groups across 2 clusters, AMI types, and scaling config fields. | Could test error propagation on the DescribeNodegroup step. | **expand**: add DescribeNodegroup error on one nodegroup — assert other nodegroups still returned with error logged (graceful degradation). |
| `tests/unit/aws_toplevel_pagination_test.go` | 75 | Broad pagination harness: EC2, Lambda, DynamoDB, IAM, KMS, CWLogs, RDS, SNS, SQS across one file; validates NextToken consumption. | Covers many services but second-page token pass-through assertions could be tighter. | **expand**: add input-token assertions. |
| `tests/unit/bug_reveal_related_column_journey_test.go` | 75 | Full journey test (menu → list → detail) verifying RELATED column appears at wide width; catches integration-level regression missed by unit tests alone. | Somewhat fragile due to deep state machine traversal. | **keep + document**: add a file-header comment enumerating the exact state transitions so future maintainers understand what each step verifies. |
| `tests/unit/detail_width_after_refresh_test.go` | 75 | Catches the `m.rightColWidth` vs `currentRightColWidth()` bug in Ctrl+R and ResetRightColumn paths — overflow measurable by ANSI-aware width comparison. | Tests were written to fail before fix; depends on fix landing. | **land the fix**: dispatch coder to land the `currentRightColWidth()` fix; the tests flip from red to green and become a permanent regression guard. |
| `tests/unit/ec2_stories_rightcol_misc_test.go` | 75 | Covers right-column toggle, tab-focus, stacked layout, narrow terminal, copy, Ctrl+R, help, YAML nav — broad surface area of detail view behavior. | Some FAILS-NOW tests (h/l, stacked, copy, Ctrl+R) may give false signal in CI. | **audit + land fixes**: go through the FAILS-NOW list; for each, either land the fix or mark the test as Skip with a link to the tracking issue. No in-between. |
| `tests/unit/qa_cache_invalidation_test.go` | 75 | TDD tests for cache invalidation on profile/region switch — cache is a correctness-critical feature where stale data is a real user bug; hard assertions on cmd==nil/non-nil. | gap: partial read — scored on first 80 lines only | **keep**. |
| `tests/unit/qa_list_rawstruct_child_views_test.go` | 75 | Verifies list rendering with RawStruct-populated child view resources (LogStream, ECS task, ASG, etc.) — catches column extraction bugs when RawStruct path resolution differs from Fields fallback. | gap: partial read | **keep**. |
| `tests/unit/qa_pagination_root_test.go` | 75 | Tests root-model wiring for IsTruncated title rendering and the probe-truncation bug (Bug 3); has intentionally skipped cache tests that document future requirements. | Skipped tests are deferred — value drops when skips are never removed. | **unblock skipped tests**: either land the cache contract so the skips can be removed, or convert them to `t.Log` comments explaining what needs to happen next. No lingering skips. |
| `tests/unit/rightcolumn_test.go` | 75 | Tests right-column rendering, toggle, count display, error display, empty-defs hint; comprehensive view-layer coverage. | Nothing obvious. | **keep**. |
| `tests/unit/views_help_resource_wiring_test.go` | 75 | Tests that NewHelp (broken path) suppresses the legend and NewHelpWithResource enables it; documents two broken call sites in app_handlers.go. | Nothing obvious — wiring test documents a real bug. | **land the fix**: dispatch coder to fix the two call sites in `app_handlers.go` to use `NewHelpWithResource`. Test flips to permanent regression guard. |
| `tests/unit/aws_ecs_svc_tasks_test.go` | 76 | Dual-status pattern (RUNNING+STOPPED), batching at 100 verified with custom mock, pagination across 150 tasks, ARN-extraction computed fields, continuation-token non-forwarding assertion — unique complex behavior. | Gap: no test for Fargate vs EC2 launch type affecting which fields are populated | **expand**: add Fargate vs EC2 launch-type fixtures — Fargate has no Containers[].NetworkBindings, EC2 has Ec2InstanceId populated. Assert distinct field sets. |
| `tests/unit/aws_lambda_invocations_test.go` | 76 | Cold-start detection, newest-first sort order, multi-invocation uniqueness, ResourceNotFoundException → empty result (important edge case), continuation-token forwarding, pagination via multi-page mock — high-value logic coverage. | Gap: no test for the TIMEOUT status parsing from "Status: timeout" in REPORT line | **expand**: add REPORT-line fixtures with "Status: timeout", "Status: error", "Status: success" → assert status field extraction. |
| `tests/unit/bug_report_regressions_test.go` | 76 | Multi-bug regression file covering right-column filter, zero-count tab guard, first-toggle behavior, and EBS volume display — each catches a distinct, non-trivial behavioral contract. | Nothing significant. | **keep**. |
| `tests/unit/detail_navigable_test.go` | 76 | Navigable field rendering (underline style on/off), Enter emits RelatedNavigateMsg for navigable field, no emit for non-navigable field; catches styling and routing bugs. | Nothing significant. | **keep**. |
| `tests/unit/detail_review_fixes_test.go` | 76 | Six review-fix behavioral assertions including Tab on auto-shown panel, independent field cursor, Enter blocked on unavailable rows, and Count=-1 rendering — all are realistic regression targets. | Nothing significant. | **keep**. |
| `tests/unit/qa_26_search_core_test.go` | 76 | Cross-view search activation/confirmation/cancellation, match counter, empty/no-match states via root model end-to-end — exercises a novel search feature's full lifecycle. | gap: partial read; no assertions on match count accuracy | **expand**: add exact match-count assertions for deterministic fixtures (e.g., "instance" appears 7 times → counter shows "7/7"). |
| `tests/unit/qa_26_search_highlight_test.go` | 76 | SearchModel component-level: all-matches highlighted, ANSI sequence presence, navigation n/N moves, case-insensitive matching, ANSI-aware search — tests correct SearchModel behavior directly. | gap: partial read | **keep**. |
| `tests/unit/qa_detail_child_views_test.go` | 76 | Child view detail rendering for 20+ resource types (LogStream, ASG, ECS, ELB, etc.) using realistic SDK structs — catches view-config path bugs at the child-type level. | gap: partial read | **keep**. |
| `tests/unit/qa_pagination_stories_test.go` | 76 | Covers QA stories D/F/G/H/I including refresh-resets-pagination and sort-preservation under pagination; catches bugs that per-fetcher tests miss entirely. | Demo-mode test relies on fixture registration order which could be brittle. | **destabilize demo test**: replace order-dependent fixture assertions with content-based lookups (find resource by ID, not by index). |
| `tests/unit/related_navigate_count_spec008_test.go` | 76 | Tests handleRelatedNavigate count=1 → direct detail and count>1 → filtered list bugs; documents and guards both paths. | Several tests are FAILS-NOW until the coder fixes app_handlers.go. | **land the fix**: dispatch coder to fix `handleRelatedNavigate`. |
| `tests/unit/related_navigate_partial_cache_test.go` | 76 | Tests partial-cache truncation bug in handleRelatedNavigate's multi-RelatedIDs branch; one test FAILS-NOW against current code. | Nothing obvious. | **land the fix**. |
| `tests/unit/views_resourcelist_sort_indicator_test.go` | 76 | Tests that exactly one sort glyph appears per active sort column, catching the dual-glyph bug for ct-events TIME/EVENT column substring match. | Nothing obvious — catches a concrete multi-column glyph bug. | **keep**. |
| `tests/unit/ctdetail_target_test.go` | 77 | Tests ExtractTarget de-duplication logic; catches the bug class where TARGET extraction produces rows that overlap with PARAMS section. File exceeded read limit but pattern is solid. | Nothing significant. | **keep**. |
| `tests/unit/issue236_truncated_zero_nav_test.go` | 77 | Tests that truncated-zero rows are not treated as confirmed-empty and that cursor can land on them; catches premature zero-count elision. | Nothing obvious. | **keep**. |
| `tests/unit/qa_keymatch_dual_path_test.go` | 77 | Directly targets BT v2 dual key-handling path bug: KeyPressMsg vs KeyReleaseMsg both documented with comments; tests the exact untested KeyMsg code path in detail.Update(). | gap: partial read | **keep** — BT v2 key dispatch is a known regression surface. |
| `tests/unit/qa_list_rawstruct_test.go` | 77 | Comprehensive RawStruct list rendering for ALL resource types using realistic SDK structs — the broadest column-extraction regression test in the suite. | gap: partial read | **keep** — this is the single most valuable cross-cutting test in the suite. |
| `tests/unit/aws_ct_events_format_test.go` | 78 | Tests `FormatCTTimestamp` (15-char invariant across boundary dates) and `FormatCTTarget` ARN-stripping with cross-account exception. Exact string output assertions catch format regressions. |  | **keep**. |
| `tests/unit/aws_ct_events_status_test.go` | 78 | Tests status + verb pair for specific events (CreateBucket→W→ct-attention). Verifies verb and status together, catching split-mapping bugs the severity test alone can't isolate. |  | **keep**. |
| `tests/unit/aws_retry_test.go` | 78 | Tests first-attempt success, throttle→retry→success, and exhaust-retries; validates call counts and delay bounds using smithy error codes. | Could test context cancellation mid-retry. | **expand**: add context-cancelled-mid-retry test — verify retry loop aborts with ctx.Err() and does not call underlying fn again. |
| `tests/unit/bug_detail_refresh_resets_rightcol_test.go` | 78 | Pins the specific contract that Ctrl+R must reset stale right-column counts; tests a realistic user-visible regression path (stale counts after refresh). | Nothing significant. | **keep**. |
| `tests/unit/config_ct_events_layout_test.go` | 78 | Pins exact column widths and detail field list for ct-events against the design spec — catches any silent layout drift in defaults_monitoring.go with exact assertions. | Nothing significant. | **keep**. |
| `tests/unit/detail_focus_test.go` | 78 | Tab-key focus state machine (auto-shown → visible → focused), Esc-unfocuses, Enter-navigates, zero-count guard; covers a multi-state system prone to regressions. | Nothing significant. | **keep**. |
| `tests/unit/ec2_stories_cursor_enter_test.go` | 78 | Tests cursor movement, g/G jump, Enter navigation on navigable/non-navigable fields, section headers — regression-prone code paths. | Some tests guarded by `//go:build spec008` so they skip in CI until the getter lands. | **unblock**: land `FieldCursor()` getter and remove build tag. |
| `tests/unit/golden_research_related_test.go` | 78 | Cross-references P0 relationships in design docs against RegisterRelated entries; catches docs/code drift automatically. | Gap: only P0 checked; P1 drift is undetected. | **expand**: extend to P1 relationships (and P2 if documented). Failure should point at the specific design-doc section that needs to match. |
| `tests/unit/issue234_fetcher_pagination_honesty_test.go` | 78 | Tests that eks/kms/ng fetchers stop after one page and report IsTruncated honestly; catches unbounded fetcher loop regressions. | Nothing obvious. | **keep**. |
| `tests/unit/qa_api_error_test.go` | 78 | Tests full APIError lifecycle: flash shown, loading cleared, Smithy error classification (ExpiredToken/AccessDenied), ClearFlashMsg auto-dismiss, S3 refresh context preservation across 3 levels — high regression value. | gap: A10 isolation test is a soft check | **tighten A10**: convert the soft check to a hard assertion (or delete if non-deterministic). |
| `tests/unit/qa_child_pagination_test.go` | 78 | All 19 child fetchers × 4 cases (first page, continuation, empty, error) — IsTruncated/NextToken contract is regression-prone after each fetcher migration. | gap: mock setup is voluminous but the logic under test is straightforward | **refactor**: extract a table-driven test runner so the 19×4 = 76 cases share mock setup. Same coverage, ~80% less code. |
| `tests/unit/qa_name_column_first_test.go` | 78 | Catches real column-order regression across 14 resource types in both config defaults and type definitions; exact key/title/count assertions will catch any swap reversal. | Nothing obvious. | **keep + generalize**: extend to all resource types, not just 14. The invariant ("first column is Name") should hold universally. |
| `tests/unit/qa_pagination_view_test.go` | 78 | Tests FrameTitle with truncated/loading/filtered states, Append=true accumulation, and 'M' key LoadMore dispatch; catches view-layer pagination state bugs. | No test for pressing 'M' when already at last page (IsTruncated=false). | **expand**: add 'M' key with IsTruncated=false → assert no cmd is dispatched and a "no more results" flash is shown. |
| `tests/unit/views_detail_render_color_tier_test.go` | 78 | Tests ColorTier branch in renderFromFieldList (ct-info/ct-attention/ct-danger color mapping); FAIL until T028. | High value: new renderer color logic, no existing coverage. | **land T028** — dispatch coder. |
| `tests/unit/views_detail_render_section_test.go` | 78 | Tests IsSection case in renderFromFieldList; FAIL until T028 adds the renderer branch; no existing coverage. | High value: new renderer branch. | **land T028** — dispatch coder. |
| `tests/unit/views_help_ct_events_legend_test.go` | 78 | TDD tests for CT events legend in help view — gating, required glyphs, severity labels, absent obsolete sections; tests FAIL until coder ships the feature. | High value for a net-new feature with no existing coverage. | **land feature** — dispatch coder; test becomes permanent guard. |
| `tests/unit/views_resourcelist_dim_filter_test.go` | 78 | Tests ctrl+z attention-only filter toggle across ct-events and standard resource types; catches filter-toggle state and view-output regressions. | Tests FAIL until coder ships the attentionOnly feature. | **land feature** — dispatch coder. |
| `tests/unit/ctdetail_parse_test.go` | 79 | Tests Parse() across all userIdentity variants with realistic CloudTrail JSON; catches field-mapping bugs in JSON deserialization for each identity type. | File exceeds read limit so only first 80 lines seen, but the coverage pattern is solid. | **keep**. |
| `tests/unit/ctdetail_sections_test.go` | 79 | BuildSections ordering and content tests covering section presence, row content, and error handling; exercises the critical section assembly logic for the CT detail view. | File exceeds read limit but the visible structure shows real behavioral coverage. | **keep**. |
| `tests/unit/detail_source_id_guard_test.go` | 79 | Catches the SourceResourceID guard bug where late async results from instance A can update instance B's right column — a realistic race condition in navigation. | Nothing significant. | **keep**. |
| `tests/unit/issue235_related_check_race_test.go` | 79 | Race-condition regression test for shared cache variable across closures; must be run with `-race` to be effective. | Gap: not in default `go test` run (requires `-race` flag). | **add -race to CI**: dispatch pm to add `-race` to the unit-test CI step; otherwise this test is decorative. |
| `tests/unit/qa_bottom_hints_test.go` | 79 | Tests BottomHints() for all 4 view models with exact KeyHint slice ordering; multiple child-key combinations, reveal registration, pagination meta — directly catches missing/wrong hints. | gap: Detail tab/right-col test uses a soft "no tab" assertion path only | **tighten Detail tab/right-col**: replace the soft "no tab" check with an exact-slice assertion on the Detail BottomHints() output for each known right-col state. |
| `tests/unit/qa_horizontal_scroll_test.go` | 79 | Directly targets diagnosed bug #105 (fitColumns shrinks vs drops last column preventing scroll right); exact test setup at the specific width that triggers the bug — high regression value. | nothing | **keep**. |
| `tests/unit/views_detail_ct_events_test.go` | 79 | TDD tests for CT events branch in DetailModel.buildFieldList — section headers, field grouping, JSON parse; FAIL until T027 lands. | High value: new code path with no existing coverage. | **land T027** — dispatch coder. |
| `tests/unit/aws_ct_events_target_fallback_test.go` | 80 | Tests the nil-JSON-blob fallback to `event.Resources` for target extraction — a real path that was broken and documented in the file header. |  | **keep**. |
| `tests/unit/aws_ssm_reveal_test.go` | 80 | TDD-style tests for RevealSSMParameter: value extraction, WithDecryption flag enforcement, error passthrough, and nil-Parameter guard — catches a real security-correctness bug (unencrypted fetch). | Nothing; tests are tight. | **keep**. |
| `tests/unit/detail_rendering_spec007_test.go` | 80 | Five specific rendering bugs (│ separator, sub-field styling, cursor row highlight, navigable underline on selected row, stacked layout at 80-99) with TDD red-first intent. | Tests are written to fail before fixes — value depends on fixes landing. | **land the 5 fixes** — dispatch coder to complete spec-007. |
| `tests/unit/ec2_stories_nav_chains_test.go` | 80 | Tests multi-hop navigation chains (count=1 → direct detail, RelatedIDs → filtered list, Esc chain) against documented bugs; high regression value. | Several tests documented as FAILS-NOW until `handleRelatedNavigate` is fixed. | **land handleRelatedNavigate fix** — dispatch coder. |
| `tests/unit/qa_correctness_bugs_test.go` | 80 | Tests 4 diagnosed bugs (LoadFromDirs field-merge, availability probe race, profile switch pre-commit, AWS_REGION env bypass) — all regression-prone with hard assertions. | gap: partial read — scored on first 60 lines | **keep**. |
| `tests/unit/related_cache_bug_test.go` | 80 | Reveals the NeedsRelatedCheck always-true bug on re-entry; guards against stale-cache and unnecessary re-dispatch regressions. | High value for cache invalidation logic. | **keep**. |
| `tests/unit/related_navigate_cache_detail_init_test.go` | 80 | Tests that cache-hit navigation branches dispatch related checks; catches permanent-loading-state bug in right column after cached navigation. | High value: three FAILS-NOW tests document a real, distinct bug. | **land the fix** — dispatch coder. |
| `tests/unit/aws_ct_events_invariants_test.go` | 82 | Cross-cutting property: every sensitive-read entry must classify as verb=R, and unknown service must not accidentally match. Catches structural bugs that per-entry tests miss. |  | **keep**. |
| `tests/unit/aws_s3_n1_test.go` | 82 | Regression test for issue #220 N+1 API calls; uses a custom `RoundTripper` to count `GetBucketNotificationConfiguration` calls — catches a real performance bug with no other coverage. | Nothing; high-value regression guard. | **keep**. |
| `tests/unit/cache_esc_pops_test.go` | 82 | Tests a specific cache-poisoning bug (EscPops=true guard missing); the scenario is realistic, the failure mode is silent data corruption, and the test would fail without the exact guard fix. | Nothing significant. | **keep**. |
| `tests/unit/ctdetail_actor_test.go` | 82 | 12 userIdentity variants × exact substring assertions on Actor(); covers all taxonomy cases from design §4 including edge cases (empty identity, cross-account, IRSA). | Nothing significant. | **keep**. |
| `tests/unit/demo_infrastructure_integrity_test.go` | 82 | Five-part comprehensive contract: fixture presence, detail-field resolution, navigable-field cross-ref, related-demo checker hits, named constants — catches fixture/registry drift across the whole demo infrastructure. | nothing (tight) | **keep** — load-bearing cross-cutting contract. Other `demo_*` files that duplicate subsets of this should be folded in per their own action plans. |
| `tests/unit/golden_demo_related_test.go` | 82 | Validates demo checker ResourceIDs resolve in demo fixtures, live checkers are non-nil, navigation roundtrips succeed — catches multiple silent failure modes. | High value: catches cross-layer inconsistencies no single-unit test covers. | **keep**. |
| `tests/unit/issue230_ctrl_r_cache_clear_test.go` | 82 | Catches the specific Ctrl+R stale-cache regression (dead handler in detail.go); documents and guards both the cache-clear and the re-dispatch paths. | High value: targeted regression for a real, documented bug. | **keep**. |
| `tests/unit/qa_architect_bugs_test.go` | 82 | Directly targets real diagnosed bugs: S3 folder-vs-detail nav ambiguity, wrong ViewDef selected from config when multiple resource types present — both are regression-prone and production-impactful. | nothing | **keep**. |
| `tests/unit/qa_pagination_child_audit_test.go` | 82 | Verifies two-call Load More flow for 11 distinct child fetchers using real AWS SDK types; catches token-forwarding bugs that single-page tests miss. | Gap: no test for when page 2 returns an error mid-stream. | **expand**: add page-2-returns-error case — assert page-1 results are preserved and error surfaces as APIError. |
| `tests/unit/related_smoketest_table_test.go` | 82 | Table-driven smoke test for all resource types covering right-column visibility, labels, counts, Tab+Enter navigation, and checker registration; catches per-type wiring regressions. | High value: consolidates 19 per-type smoke tests into one table. | **keep**. |
| `tests/unit/aws_ct_events_cross_account_actor_test.go` | 83 | Tests §1.4 cross-account actor prefix format (`<acct>/<actor>`) for IAMUser, Root, and AssumedRole identity types. Catches the specific `[cross] ` legacy prefix regression. |  | **keep**. |
| `tests/unit/issue233_empty_truncated_cache_test.go` | 83 | Tests empty-but-truncated cold-cache write-back bug (three-step corruption path); catches silent metadata drop in app.go. | High value: regression guard for a subtle multi-step state corruption. | **keep**. |
| `tests/unit/issue237_239_240_241_related_fixes_test.go` | 83 | Four distinct related-view infrastructure bugs (NextToken preservation, stale-generation discard, field-only checker cold-fetch suppression, concurrent probe cap); each catches a real regression. | High value: multi-bug contract test. | **keep**. |
| `tests/unit/qa_detail_paths_test.go` | 83 | Tests every detail path from views.yaml against realistic SDK struct fixtures for ALL resource types — this is the highest-value cross-cutting test, catching wrong field paths and nil extraction misses. | nothing | **keep** — single most valuable test in the suite. Protect with a `//go:nolint` on any linter that might shrink it. |
| `tests/unit/column_key_mismatch_test.go` | 84 | High-value registry sweep: verifies every column Key in every ResourceTypeDef has a matching field key registered by the fetcher — catches an entire class of silent mapping bugs whenever a new resource type is added. | Nothing significant. | **keep**. |
| `tests/unit/issue232_truncation_contract_test.go` | 84 | Tests that related checkers return Count=-1 when cache is truncated and zero local matches found; catches IsTruncated discard bugs across 4+ checker types. | High value: multi-checker correctness contract against a subtle pagination bug. | **keep**. |
| `tests/unit/aws_ct_events_verb_classification_test.go` | 85 | Comprehensive §2.1 table-driven test covering Batch*/Decrypt/Encrypt/Sign/ReEncrypt bug-fix cases explicitly. Catches the specific "BatchGet → W" and "Decrypt → W" classification bugs named in the file. |  | **keep**. |
| `tests/unit/aws_ct_events_review_fixes_test.go` | 87 | Locks five specific code-review bugs: sort-across-month-boundary, BatchDelete→D, BatchGetItem target joining, cross-account Root prefix, and empty-localAccount FormatCTTarget. Each test was written to fail on current code — extremely high regression value. |  | **keep**. |
| `tests/unit/aws_ct_events_redesign_test.go` | 88 | High-value TDD file locking the full `ClassifyCTVerb` table (R/W/D/I/N/S/? classifications), `_ct.*` field flattening, and verb→status mapping. Catches exactly the misclassification bugs documented in the comments. |  | **keep**. |
| `tests/unit/aws_ct_events_severity_test.go` | 91 | Property test locking the 38-entry §1.3 sensitive-reads allowlist, all three severity tiers with precedence rules, §1.4 regression guards for AssumeRole* and noisy reads. Exceptionally high value — every incorrect allowlist membership or precedence inversion fails here. |  | **keep**. |
