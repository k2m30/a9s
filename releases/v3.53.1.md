# v3.53.1 — Patch

Internal cleanup only — no behavior change. A duplication audit across the
mwaa / transfer / lt / vpc-peer series cut ~340 lines of redundant code whose
runtime behavior is identical:

- Four copies of the "details denied" degraded-row finding collapse into one
  shared builder; each resource keeps its own wording.
- Per-state finding `switch`es became lookup tables; the launch-template
  healthy/degraded builders, the vpc-peer side accessors, and three copies of
  the Lambda-ARN→name helper each merged into one.
- Four byte-identical row-color functions folded into a single shared helper.
- Demo fixtures and fakes de-duplicated; a DynamoDB fake now reports an access
  denial as a typed AWS error, like every other fake.

All read-only guarantees, status phrases, related-panel counts, and findings
are unchanged and remain pinned by the existing test suite.
