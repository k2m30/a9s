## Bug Fixes

- **Docker**: Fix container home directory permissions — `COPY` created `/home/a9s` owned by root, preventing config directory creation (#78)
- **Docker**: Fix Tokyo Night Dark theme color degradation — `FROM scratch` ships zero env vars, so Lipgloss degrades all hex colors to ANSI-256 purple. Set `TERM` and `COLORTERM` (#79)
