# v3.25.0 — CloudTrail Structured Fields

CloudTrail Events now parse the JSON `CloudTrailEvent` blob into typed fields for the detail and YAML views.

## What's New

Fields like EventName, EventSource, SourceIPAddress, UserAgent, and ErrorCode are extracted from the raw JSON and displayed as first-class fields (#117, #118). Previously these were only visible as a raw JSON string.
