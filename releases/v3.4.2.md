## a9s v3.4.2

### Fixes

- **NO_COLOR cursor visibility** -- cursor was invisible in `NO_COLOR=1` mode because `RowSelected` was reset to a zero-value style. Now uses ANSI reverse video (`Reverse(true)`) to invert terminal colors without specifying any color values, keeping navigation fully usable. (#74)
