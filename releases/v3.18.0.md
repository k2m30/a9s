# v3.18.0 — Per-Resource View Config & Dependency Bumps

## What's Changed

- Split monolithic `views.yaml` into per-resource YAML files under `.a9s/views/` for cleaner config management
- Added `cmd/viewsgen/` tool to auto-generate view config files from built-in defaults
- Bumped GitHub Actions versions: checkout, setup-go, upload-artifact, deploy-pages (#91, #92, #93, #94, #95)
