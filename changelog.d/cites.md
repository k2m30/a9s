## Fixed

- The per-resource docs now point at the signals page as it is written today:
  every reference names a category and a row, or a section heading, instead of
  a line number or a table cell that no longer exists.
- The EKS cluster and CloudTrail event docs now list the wordings and
  severities the app actually shows, so the doc and the screen agree.
- The CodeArtifact doc says what the public-access check decides: the
  repository policy is parsed and evaluated, not searched for a literal
  `"Principal":"*"`.
